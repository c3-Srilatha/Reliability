/**
 * Bundler test of an inner type that defines ts functions and is mixed into another type that
 * will inherit its ts implementations.
 */
@typeScript
type BundlerTestAppInheritanceChild.Inner {
  foo: function(): string ts-client
}
