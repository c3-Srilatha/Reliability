// TypeScript definitions for the C3 type EvalMetricsResult

/**
 * Results from metric evaluation operations in the C3 AI Agentic Platform.
 *
 * In the C3 AI Agentic Platform, EvalMetricsResult contains the output from evaluating metrics
 * using {@link MetricEvaluatable#evalMetrics} or {@link MetricEvaluatable#evalMetricsWithMetadata}.
 * Provides structured access to {@link Timeseries} data, evaluation plans, error information,
 * and utilities for data transformation and visualization.
 *
 * Key Use Cases:
 * 1. Metric Analysis: Access computed metric values across multiple sources and time periods
 * 2. Data Visualization: Transform results for charts and dashboards using built-in renderers
 * 3. Error Handling: Track evaluation failures when using continueOnError option
 * 4. Performance Monitoring: Analyze evaluation plans and execution timing details
 * 5. Data Export: Convert results to various formats (DataTable, pandas DataFrame)
 *
 * Result Structure:
 * - Primary data organized as: sourceId -> resultKey -> {@link Timeseries}
 * - Alternative view available via {@link #byMetrics}: resultKey -> sourceId -> {@link Timeseries}
 * - Error tracking per source when evaluation failures occur
 * - Optional evaluation plan for performance analysis and debugging
 *
 * IMPORTANT: Result Key Customization
 * The inner map key in {@link #result} serves as the identifier for each evaluated metric.
 * By default, this key is the metric name from the expression. However, when
 * {@link EvalMetricsSpec#resultKey} is specified in the evaluation specification,
 * this custom lambda function generates the result keys, allowing for dynamic key naming based
 * on expression names, bindings, and other evaluation context.
 *
 * Integration Patterns:
 * - Triggered by {@link MetricEvaluatable} implementations on entity Types
 * - Consumed by UI components for metric dashboards and analytics
 * - Processed by data pipelines for metric aggregation and reporting
 * - Used with {@link EvalMetricsSpec} for configuring evaluation parameters
 *
 * Performance Characteristics:
 * - Optimized for read access to metric data across multiple sources
 * - Supports efficient data flattening and transformation operations
 * - Memory-efficient storage of large metric result sets
 * - Built-in caching support through evaluation specifications
 *
 * Related Types:
 * - {@link EvalMetricsSpec} - Configuration for metric evaluation operations
 * - {@link MetricEvaluatable} - Interface for Types that support metric evaluation
 * - {@link Timeseries} - Individual metric value sequences with temporal data
 * - {@link EvalPlan} - Execution plan details for performance analysis
 *
 * @see EvalMetricsSpec
 * @see MetricEvaluatable
 * @see Timeseries
 * @see EvalPlan
 * @see metrics-evaluating-metrics.c3doc.md
 * @see metrics-bindings.c3doc.md
 * @see metrics-home.c3doc.md
 *
 * @remarks this represents a value passed to a method that expects an instance of EvalMetricsResult
 */
declare interface IEvalMetricsResult {

  /**
   * Primary metric evaluation results organized by source and result identifiers.
   *
   * Structure: `map<sourceId, map<resultKey, Timeseries>>`
   * - Outer map key: ID of the source object on which metrics were evaluated
   * - Inner map key: Result identifier for each metric, which can be either:
   *   1. The original metric name from the expression (default behavior)
   *   2. A custom key generated by {@link EvalMetricsSpec#resultKey} lambda function
   *
   * When accessing specific metrics (e.g., via {@link #timeseries}), use the inner map key
   * as the identifier. This key may differ from the original metric name when
   * {@link EvalMetricsSpec#resultKey} is specified in the evaluation specification.
   *
   * @see EvalMetricsSpec#resultKey
   * @see #timeseries
   * @see #byMetrics
   */
  result?: C3.Map<string | null, C3.Map<string | null, any>> | {[key: string | null]: C3.Map<string | null, any> | {[key: string | null]: any}};

  /**
   * Detailed evaluation plan containing execution metadata and performance analysis.
   *
   * Populated when {@link EvalMetricsSpec#explain} is set to true in the evaluation specification.
   * Contains information about expression parsing, metric resolution, execution timing, and
   * optimization details. Useful for debugging performance issues and understanding metric
   * evaluation behavior.
   *
   * @see EvalMetricsSpec#explain
   * @see EvalPlan
   */
  evalPlan?: IEvalPlan | null;

  /**
   * Error information for failed metric evaluations by source object.
   *
   * Structure: sourceId -> error message
   * Populated only when {@link EvalMetricsSpec#continueOnError} is set to true, allowing
   * evaluation to continue despite failures for specific source/metric combinations.
   * Sources with successful evaluations will not appear in this map.
   *
   * @see EvalMetricsSpec#continueOnError
   */
  errors?: C3.Map<string | null, string | null> | {[key: string | null]: string | null};

  /**
   * Localized display labels for metrics organized by result key.
   *
   * Structure: `map<resultKey, translatedLabel>`
   * Populated when {@link EvalMetricsSpec#locale} is specified in the evaluation
   * specification. Maps the same keys used in {@link #result} to their translated,
   * human-readable labels in the requested locale.
   *
   * Note: The keys in this map correspond to the inner map keys from {@link #result},
   * which may be customized by {@link EvalMetricsSpec#resultKey}, not necessarily
   * the original metric names.
   *
   * @see EvalMetricsSpec#locale
   * @see EvalMetricsSpec#resultKey
   * @see Translation
   */
  translations?: C3.Map<string | null, string | null> | {[key: string | null]: string | null};
}

/**
 * Results from metric evaluation operations in the C3 AI Agentic Platform.
 *
 * In the C3 AI Agentic Platform, EvalMetricsResult contains the output from evaluating metrics
 * using {@link MetricEvaluatable#evalMetrics} or {@link MetricEvaluatable#evalMetricsWithMetadata}.
 * Provides structured access to {@link Timeseries} data, evaluation plans, error information,
 * and utilities for data transformation and visualization.
 *
 * Key Use Cases:
 * 1. Metric Analysis: Access computed metric values across multiple sources and time periods
 * 2. Data Visualization: Transform results for charts and dashboards using built-in renderers
 * 3. Error Handling: Track evaluation failures when using continueOnError option
 * 4. Performance Monitoring: Analyze evaluation plans and execution timing details
 * 5. Data Export: Convert results to various formats (DataTable, pandas DataFrame)
 *
 * Result Structure:
 * - Primary data organized as: sourceId -> resultKey -> {@link Timeseries}
 * - Alternative view available via {@link #byMetrics}: resultKey -> sourceId -> {@link Timeseries}
 * - Error tracking per source when evaluation failures occur
 * - Optional evaluation plan for performance analysis and debugging
 *
 * IMPORTANT: Result Key Customization
 * The inner map key in {@link #result} serves as the identifier for each evaluated metric.
 * By default, this key is the metric name from the expression. However, when
 * {@link EvalMetricsSpec#resultKey} is specified in the evaluation specification,
 * this custom lambda function generates the result keys, allowing for dynamic key naming based
 * on expression names, bindings, and other evaluation context.
 *
 * Integration Patterns:
 * - Triggered by {@link MetricEvaluatable} implementations on entity Types
 * - Consumed by UI components for metric dashboards and analytics
 * - Processed by data pipelines for metric aggregation and reporting
 * - Used with {@link EvalMetricsSpec} for configuring evaluation parameters
 *
 * Performance Characteristics:
 * - Optimized for read access to metric data across multiple sources
 * - Supports efficient data flattening and transformation operations
 * - Memory-efficient storage of large metric result sets
 * - Built-in caching support through evaluation specifications
 *
 * Related Types:
 * - {@link EvalMetricsSpec} - Configuration for metric evaluation operations
 * - {@link MetricEvaluatable} - Interface for Types that support metric evaluation
 * - {@link Timeseries} - Individual metric value sequences with temporal data
 * - {@link EvalPlan} - Execution plan details for performance analysis
 *
 * @see EvalMetricsSpec
 * @see MetricEvaluatable
 * @see Timeseries
 * @see EvalPlan
 * @see metrics-evaluating-metrics.c3doc.md
 * @see metrics-bindings.c3doc.md
 * @see metrics-home.c3doc.md
 *
 * @remarks this represents a made instance of EvalMetricsResult
 */
declare class EvalMetricsResult extends Obj {

  /**
   * Primary metric evaluation results organized by source and result identifiers.
   *
   * Structure: `map<sourceId, map<resultKey, Timeseries>>`
   * - Outer map key: ID of the source object on which metrics were evaluated
   * - Inner map key: Result identifier for each metric, which can be either:
   *   1. The original metric name from the expression (default behavior)
   *   2. A custom key generated by {@link EvalMetricsSpec#resultKey} lambda function
   *
   * When accessing specific metrics (e.g., via {@link #timeseries}), use the inner map key
   * as the identifier. This key may differ from the original metric name when
   * {@link EvalMetricsSpec#resultKey} is specified in the evaluation specification.
   *
   * @see EvalMetricsSpec#resultKey
   * @see #timeseries
   * @see #byMetrics
   */
  readonly result?: C3.Map<string | null, C3.Map<string | null, any>>;
  withResult(result: C3.Map<string | null, C3.Map<string | null, any>> | {[key: string | null]: C3.Map<string | null, any> | {[key: string | null]: any}}): EvalMetricsResult;

  /**
   * Detailed evaluation plan containing execution metadata and performance analysis.
   *
   * Populated when {@link EvalMetricsSpec#explain} is set to true in the evaluation specification.
   * Contains information about expression parsing, metric resolution, execution timing, and
   * optimization details. Useful for debugging performance issues and understanding metric
   * evaluation behavior.
   *
   * @see EvalMetricsSpec#explain
   * @see EvalPlan
   */
  readonly evalPlan?: EvalPlan | null;
  withEvalPlan(evalPlan: IEvalPlan | null): EvalMetricsResult;

  /**
   * Error information for failed metric evaluations by source object.
   *
   * Structure: sourceId -> error message
   * Populated only when {@link EvalMetricsSpec#continueOnError} is set to true, allowing
   * evaluation to continue despite failures for specific source/metric combinations.
   * Sources with successful evaluations will not appear in this map.
   *
   * @see EvalMetricsSpec#continueOnError
   */
  readonly errors?: C3.Map<string | null, string | null>;
  withErrors(errors: C3.Map<string | null, string | null> | {[key: string | null]: string | null}): EvalMetricsResult;

  /**
   * Localized display labels for metrics organized by result key.
   *
   * Structure: `map<resultKey, translatedLabel>`
   * Populated when {@link EvalMetricsSpec#locale} is specified in the evaluation
   * specification. Maps the same keys used in {@link #result} to their translated,
   * human-readable labels in the requested locale.
   *
   * Note: The keys in this map correspond to the inner map keys from {@link #result},
   * which may be customized by {@link EvalMetricsSpec#resultKey}, not necessarily
   * the original metric names.
   *
   * @see EvalMetricsSpec#locale
   * @see EvalMetricsSpec#resultKey
   * @see Translation
   */
  readonly translations?: C3.Map<string | null, string | null>;
  withTranslations(translations: C3.Map<string | null, string | null> | {[key: string | null]: string | null}): EvalMetricsResult;

  /**
   * Client-side visualization component for rendering metric results as charts.
   *
   * Provides built-in charting capabilities to visualize all {@link Timeseries}
   * from the evaluation results on a single chart. Automatically handles
   * time alignment, scaling, and legend generation for multiple metrics
   * and sources.
   *
   * @see #allTimeseries
   */
  renderer(): HtmlRenderer | null;

  /**
   * Load the JSON-based representation and reconstruct the corresponding object.
   *
   * fromJson is be called on the type be deserialized and must reconstruct an Obj of the appropriate type (which may be
   * a type that mixes in the type on which it is called). This means that the resulting object's type will be isA the
   * called-on type, but perhaps not identical. In particular, `Obj.fromJson` works for any actual type and will return
   * an instance of the correct type.
   *
   * @see #toJson
   */
  static fromJson(json: any | null): EvalMetricsResult | null;

  /**
   * Load the JSON-based representation and reconstruct the corresponding object.
   *
   * fromJsonString is be called on the type be deserialized and must reconstruct an Obj of the appropriate type (which may be
   * a type that mixes in the type on which it is called). This means that the resulting object's type will be isA the
   * called-on type, but perhaps not identical. In particular, `Obj.fromJsonString` works for any actual type and will return
   * an instance of the correct type.
   *
   * @see #toJsonString
   */
  static fromJsonString(json: string | null): EvalMetricsResult | null;

  /**
   * Load the XML-based representation and reconstruct the corresponding object.
   *
   * fromXmlString is be called on the type be deserialized and must reconstruct an Obj of the appropriate type (which
   * may be a type that mixes in the type on which it is called). This means that the resulting object's type will be
   * isA the called type, but perhaps not identical. In particular, `Obj.fromXmlString` works for any actual type and
   * will return an instance of the correct type.
   *
   * @see #toXmlString
   */
  static fromXmlString(xml: string | null): EvalMetricsResult | null;

  /**
   * Load from contentType representation and reconstruct the corresponding object.
   *
   * fromString is be called on the type be deserialized and must reconstruct an object of the appropriate type
   * (which may be a type that mixes in the type on which it is called). This means that the resulting object's type
   * will be isA the called-on type, but perhaps not identical. In particular, `fromString` works for any actual
   * type and will return an instance of the correct type.
   */
  static deserialize(contentStr: string | null, contentType: string): EvalMetricsResult | null;

  /**
   * Returns new instance with all references to old type, including result of #type, replaced with new type. If new
   * type does not contain fields from old or field value types are not convertable then drops the field.
   *
   * This method is used during live metadata update
   */
  replaceType(old: Type, new_: Type): EvalMetricsResult;

  /**
   * Result of this function call is a copy of current instance with all non empty fields replaced based on results of
   * the `mapper` invocation.
   *
   * @param action
   *           lambda to apply for every field value to produce a new value for that field
   * @param convertValue
   *           if true, attempt to convert the value to match the field's type
   */
  mapFieldValues(mapper: λBiFunction<FieldType, any, any>, convertValue?: boolean): EvalMetricsResult;

  /**
   * Result of this function call is a copy of current instance with all fields replaced based on results of the
   * `mapper` invocation.
   *
   * @param spec
   *           which fields to include
   * @param mapper
   *           lambda to apply for every field value to produce a new value for that field
   * @param convertValue
   *           if true, attempt to convert the value to match the field's type
   */
  mapFieldValues(spec: ValueSpec, mapper: λBiFunction<FieldType, any, any>, convertValue?: boolean): EvalMetricsResult;

  /**
   * Result of this function call is a copy of current instance with all non empty fields replaced based on results of
   * the asynchronous `mapper` invocation.
   *
   * @param action
   *           lambda to apply for every field value to produce a new value for that fields
   * @param convertValue
   *           if true, attempt to convert the value to match the field's type
   */
  mapFieldValuesAsync(mapper: λBiFunction<FieldType, any, C3.Promise<any> | null>, convertValue?: boolean): C3.Promise<EvalMetricsResult | null>;

  /**
   * Result of this function call is a copy of current instance with all fields replaced based on results of the
   * asynchronous `mapper` invocation.
   *
   * @param spec
   *           which fields to include
   * @param mapper
   *           lambda to apply for every field value to produce a new value for that field
   * @param convertValue
   *           if true, attempt to convert the value to match the field's type
   */
  mapFieldValuesAsync(spec: ValueSpec, mapper: λBiFunction<FieldType | null, any, C3.Promise<any> | null>, convertValue?: boolean): C3.Promise<EvalMetricsResult | null>;

  /**
   * Result of this function call is a copy of current instance with specified field value replaced based on result of
   * the `mapper` invocation.
   * @param field
   *           field being mapped
   * @param includeEmpty
   *           if set, invokes mapper for fields with empty value
   * @param mapper
   *           lambda to apply for every field value to produce a new value for that field
   * @param convertValue
   *           if true, attempt to convert the value to match the field's type
   */
  mapFieldValue(field?: FieldType | null, includeEmpty?: boolean, mapper: λFunction<any, any>, convertValue?: boolean): EvalMetricsResult;

  /**
   * Executes the specified lambda against each referenced Obj instance and replaces it's value with result of this
   * lambda application.
   *
   * Result of this function call is a copy of current instance with all references replaced based on results of the
   * `mapper` invocation.
   *
   * @param action
   *           function to be executed for each pair of field type and Obj instance
   * @param convertValue
   *           if true, attempt to convert the value to match the field's type
   */
  mapRefs(mapper: λBiFunction<FieldType, Obj, Obj | null>, convertValue?: boolean): EvalMetricsResult;

  /**
   * Executes the specified lambda against each referenced Obj instance and replaces it's value with result of this
   * lambda application.
   *
   * Result of this function call is a copy of current instance with all references replaced based on results of the
   * `mapper` invocation.
   *
   * @param includeEmpty
   *           if `true` will also process references with `null` / "Empty" references
   * @param mapper
   *           function to be executed for each pair of field type and Obj instance for producing new reference value
   * @param convertValue
   *           if true, attempt to convert the value to match the field's type
   */
  mapRefs(includeEmpty: boolean, mapper: λBiFunction<FieldType, Obj, Obj | null>, convertValue?: boolean): EvalMetricsResult;

  /**
   * Populates all missing default values and throws error if any constraint is violated.
   */
  validateObj(): EvalMetricsResult;

  /**
   * Builds a new Obj instance by adding the provided field in it. The name must correspond to an existing field
   * defined on this type or its mixins. The value must be of the correct type if doNotConvert flag is true.
   *
   * @param field
   *           name of the field
   * @param value
   *           of the field
   * @param doNotConvert
   *           if true, do not attempt to convert the value to match the field's type
   * @return new Obj
   *
   * @see #withoutField
   * @see #defaultField
   */
  withField(field: string, value: any, doNotConvert?: boolean): EvalMetricsResult;

  /**
   * Builds a new Obj instance by adding the provided field in it. The name must correspond to an existing field
   * defined on this type or its mixins. The value must be of the correct type if doNotConvert flag is true.
   *
   * @param field
   *           the field
   * @param value
   *           of the field
   * @param doNotConvert
   *           if true, do not attempt to convert the value to match the field's type
   * @return new Obj
   *
   * @see #withoutField
   * @see #defaultField
   */
  withField(field: FieldType, value: any, doNotConvert?: boolean): EvalMetricsResult;

  /**
   * Builds a new Obj instance by adding the provided fields in it. The name must correspond to an existing fields
   * defined on this type or its mixins. The values must be of the correct type if doNotConvert flag is true.
   *
   * @param fields
   *           map of field names/values
   * @param doNotConvert
   *           if true, attempt to convert the values to match the fields' type
   * @return new Obj
   */
  withFields(fields: C3.Map<string | null, any> | null, doNotConvert?: boolean): EvalMetricsResult;

  /**
   * Builds a new Obj with the value at the specified path field. If the field is null, the field #isFieldSet to null.
   * If you would like to #unsetField, you should call #withoutFieldAtPath instead.
   *
   * Immutable objects may return the same instance if the field being set does not actually represent a
   * change to the existing object.
   *
   * @param path
   *           path to set value at
   * @param value
   *           value to set
   * @param doNotConvert
   *           if true, attempt to convert the values to match the fields' type
   * @param doNotCreateIfMissing
   *           true indicates that any empty reference along the path will not set the value
   * @return new Obj
   */
  withFieldAtPath(path: string, value: any, doNotConvert?: boolean, doNotCreateIfMissing?: boolean): EvalMetricsResult;

  /**
   * Builds a new Obj without the specified path field.
   *
   * Immutable objects may return the same instance if the field being removed does not actually represent a
   * change to the existing object.
   *
   * @param path
   *           path for field to remove
   * @return new Obj
   *
   * @see #withFieldAtPath
   * @see #withoutField
   */
  withoutFieldAtPath(path: string): EvalMetricsResult;

  /**
   * Builds a new Obj, removing the field with the provided name.
   *
   * Immutable objects may return the same instance if the field being removed is not present in the existing object.
   *
   * @param field
   *           name of the field to remove
   * @return new Obj with removed field
   *
   * @see #unsetField
   * @see #removeField
   */
  withoutField(field: string | null): EvalMetricsResult;

  /**
   * Builds a new Obj, removing the field with the provided field type.
   *
   * Immutable objects may return the same instance if the field being removed is not present in the existing object.
   *
   * @param field
   *           name of the field to remove
   * @return new Obj with removed field
   *
   * @see #unsetField
   * @see #removeField
   */
  withoutField(field: FieldType | null): EvalMetricsResult;

  /**
   * Builds a new Obj, removing the fields with the provided names.
   *
   * Immutable objects may return the same instance if the fields being removed are not present in the existing object.
   *
   * @param fields
   *           names of the fields to remove
   * @return new Obj with removed fields
   */
  withoutFields(fields: C3.Array<string | null> | null): EvalMetricsResult;

  /**
   * Builds a new Obj, removing the fields with the provided field types. Be sure to use the FieldType instance for the
   * exact same type as the type of the obj to respect the "ordinal" of the field type
   *
   * Immutable objects may return the same instance if the fields being removed are not present in the existing object.
   *
   * @param fields
   *           field types to remove
   * @return new Obj with removed fields
   */
  withoutFieldsByType(fields: C3.Array<FieldType | null> | null): EvalMetricsResult;

  /**
   * @return a new Obj, removing the field types marked with annotation @config(secret=true) recursively
   */
  withoutSecretFields(): EvalMetricsResult;

  /**
   * Builds a new Obj instance by adding the default values (if defined) for all unset fields. This is implemented by
   * calling {@link FieldType#defaultValue defaultValue} for a field if it is not already set and
   * {@link FieldType#hasDefault has a default}. It will also set {@see ValueType#initialValue initial values} for
   * fields with required primitive ValueTypes (E.g. x: `!int32` -> will be set to 0). Note that this will not overwrite
   * fields that have already been set.
   *
   * {@link FunctionParam#validateArg} will call {@link #withDefaults} for {@link Spec}s passed as arguments to methods.
   * As a result, methods should be implemented assuming all default values are set on `Spec` arguments.
   *
   * @param includeEmptyRefsWithDefaults
   *           it `true` then missing / empty child references that have fields with defaults will also be instantiated
   * @param defaultFields
   *           If not empty, a list of default field paths to populate.  Any default fields not specified in the
   *           array will be ignored.
   * @return new Obj
   *
   * @see #defaultField
   * @see FieldType#defaultValueConst
   * @see FieldType#defaultValue
   */
  withDefaults(includeEmptyRefsWithDefaults?: boolean, defaultFields?: C3.Array<string | null>): EvalMetricsResult;

  /**
   * Builds a new Obj, by setting a field on this `Obj` to the field's default value. If the field has no default, this
   * method will behave the same as {@link #unsetField}.
   *
   * @param field
   *         name of the field to default
   * @return new `Obj` with the specified field set to its default value
   *
   * @see #withField
   * @see #unsetField
   */
  defaultField(field: string): EvalMetricsResult;

  /**
   * Builds a new Obj, by setting a field on this `Obj` to the fields default value. If the field has no default, this
   * method will behave the same as {@link #unsetField}.
   *
   * @param field
   *         field type to default
   * @return new `Obj` with the specified field set to its default value
   *
   * @see #withField
   * @see #unsetField
   */
  defaultField(field: FieldType): EvalMetricsResult;

  /**
   * Unsets a field from this `Obj`, meaning that the field will become not {@link #isFieldSet set}. Note that this
   * is different from {@link removeField}
   *
   * @param field
   *         name of the field to unset
   * @return new `Obj` with the specified field unset
   *
   * @see #withoutField
   * @see #removeField
   */
  unsetField(field: string): EvalMetricsResult;

  /**
   * Unsets a field from this `Obj`, meaning that the field will become not {@link #isFieldSet set}. Note that this
   * is different from {@link removeField}
   *
   * @param field
   *         field type to unset
   * @return new `Obj` with the specified field unset
   *
   * @see #withoutField
   * @see #removeField
   */
  unsetField(field: FieldType): EvalMetricsResult;

  /**
   * Removes a field from this `Obj`, meaning that the field will become {@link isFieldMissing missing}. Note that this
   * is different from {@link #unsetField}
   *
   * @param field
   *         name of the field to remove
   * @return new `Obj` with the specified field removed
   *
   * @see #withoutField
   * @see #unsetField
   */
  removeField(field: string): EvalMetricsResult;

  /**
   * Removes a field from this `Obj`, meaning that the field will become {@link isFieldMissing missing}. Note that this
   * is different from {@link #unsetField}
   *
   * @param field
   *         field type to remove
   * @return new `Obj` with the specified field removed
   *
   * @see #withoutField
   * @see #unsetField
   */
  removeField(field: FieldType): EvalMetricsResult;

  /**
   * Merges all the fields of the provided Obj into this instance, producing a new Obj of the same type as this one.
   * In case of conflicts, fields of other instance take precedence unless otherwise specified by the fieldPathMergeSpec
   *
   * @param other
   *           object
   * @param fieldPathMergeSpec
   *           mapping of fields of the object to the respective merge annotations
   * @return the new merged Obj
   */
  mergeObj(other: Obj | null, fieldPathMergeSpec?: C3.Map<string | null, string | null>): EvalMetricsResult;

  /**
   * Merges all the fields of the provided Obj into this instance, producing a new Obj of the same type as this one.
   * In case of conflicts, fields of other instance take precedence.
   *
   * @param other
   *           object
   * @param otherFieldsFilter
   *           only fields of otherFieldsFilter type from other are merged into this obj.
   * @return the new merged Obj
   */
  mergeObj(other: Obj | null, otherFieldsFilter: Type): EvalMetricsResult;

  /**
   * Merge the fields of this Obj with corresponding fields on other Obj using the provided lambda. This means that
   * fields that exist on other Obj and do not exist on this Obj will not be added to final Obj.
   * @param deep
   *        if set to true then traverse reference and collection fields and merge corresponding fields or elements with
   *        the same key or index.
   */
  mergeObj(other: Obj | null, deep?: boolean, merger: λQuadFunction<FieldPath | null, any, FieldPath | null, any, any>): EvalMetricsResult;

  /**
   * Merge the fields of this Obj with corresponding fields on other Obj using the provided lambda. This means that
   * fields that exist other Obj and do not exist on this Obj will not be added to final Obj or evaluated. Does not
   * traverse child reference and collection fields.
   */
  mergeObj(other: Obj | null, merger: λQuadFunction<FieldType | null, any, FieldType | null, any, any>): EvalMetricsResult;

  mergeJson(json: any | null): EvalMetricsResult;

  /**
   * Merge the obj references within the current obj
   * @param deep
   *        If set, traverses the reference fields within the obj as well for a deep merge
   * @param objKey
   *        lambda specifying how to obtain the key for the Obj while determining which Objs to merge
   * @param filter
   *        Field paths that need to be filtered from this merge
   * @return Obj with child references merged
   */
  mergeChildren(deep?: boolean, objKey?: λFunction<Obj | null, any> | null, filter?: λPredicate<string> | null): EvalMetricsResult;

  /**
   * Adds the numeric Obj fields with the other Objs respective fields.
   * If deep is set it will traverse reference and collection fields and sum corresponding numeric fields in
   * references with same name and collection elements at same index or key.
   */
  sumObj(other: Obj | null, deep?: boolean): EvalMetricsResult;

  /**
   * Build an array of the correct type with a single element which is this instance.
   *
   * @return new array instance with this as only element.
   */
  singletonArray(): C3.Array<EvalMetricsResult | null>;

  /**
   * Creates an array of instances of this type.
   */
  static array(...elements: any[]): C3.Array<EvalMetricsResult | null>;

  /**
   * Creates an array of instances of this type.
   */
  static arrayBuilder(): ArrayBuilder<EvalMetricsResult | null> | null;

  /**
   * Build an set of the correct type with a single element which is this instance.
   *
   * @return new array instance with this as only element.
   */
  singletonSet(): C3.Set<EvalMetricsResult | null>;

  /**
   * Creates a set of instances of this type.
   */
  static setBuilder(): SetBuilder<EvalMetricsResult | null> | null;

  /**
   * Create a map of string to elements of this type.
   */
  static mapBuilder(): MapBuilder<string | null, EvalMetricsResult | null> | null;

  /**
   * Create a map with the given key type and elements of this type.
   */
  static mapBuilderOf(keyType: ValueType): MapBuilder<any, EvalMetricsResult | null> | null;

  /**
   * @return new ObjBuilder with initial state set to fields of this instance.
   */
  toBuilder(): ObjBuilder<EvalMetricsResult | null>;

  /**
   * @return new ObjBuilder of this instance.
   */
  static builder(): ObjBuilder<EvalMetricsResult | null>;

  /**
   * Construct instance of this type from provided field values and options
   */
  static fromFields(fields: C3.Map<FieldType | null, any> | null, spec: Obj.MakeSpec | null): EvalMetricsResult;

  /**
   * Construct an instance of this type from provided fields
   * @param fields
   *            Fields to construct the instance of the obj with
   * @param withDefaults
   *            If set, then the Obj is made with default & initial values (required primitive fields e.g. !int32 -> 0) populated
   *
   * @see withDefaults
   */
  static fromFields(fields: C3.Map<FieldType | null, any | any | null> | null, withDefaults?: boolean): EvalMetricsResult;

  /**
   * Construct instance of this type from provided field values and options
   */
  static make(fields: C3.Map<string | null, any> | null, spec: Obj.MakeSpec | null): EvalMetricsResult;

  /**
   * Construct an instance of this type with no non-default field values unless explicitly specified by passing param true
   * @param withDefaults
   *            If set, then the Obj is made with default & initial values (required primitive fields e.g. !int32 -> 0)
   *            populated
   *
   * @see withDefaults
   */
  static make(withDefaults?: boolean): EvalMetricsResult;

  /**
   * Construct an instance from provided fields
   * @param fields
   *            Fields (in the format <field_name, value>) to construct an instance of the obj. Note that "type" as a
   *            field_name will be considered as the actual Obj's type, e.g. Obj.make({"type": "Panda"}) is equivalent
   *            to Panda.make()
   * @param withDefaults
   *            If set, then the Obj is made with default & initial values (required primitive fields e.g. !int32
   *            -> 0) populated. Passing an empty value for a field will result in the initial value being set if
   *            the field does not {@link ValueModifier#PRESERVES_EMPTY preserve empty}
   *
   *
   * @see fromFields
   * @see beforeMake
   * @see afterMake
   * @see withDefaults
   */
  static make(fields: C3.Map<string | null, any | any | null> | null, withDefaults?: boolean): EvalMetricsResult;

  /**
   * Construct an instance of this type from provided fields. Note it is more efficient to use #fromFields and other overloads
   *
   * ```js
   * User.make({
   *   email: 'joe@smith.com',
   *   realName: 'Joe Smith'
   * })
   *
   * Obj.make({
   *   type: 'User',
   *   email: 'joe@smith.com',
   *   realName: 'Joe Smith'
   * })
   * ```
   *
   * ```py
   * c3.User.make({
   *   "email": 'joe@smith.com',
   *   "realName": 'Joe Smith'
   * })
   *
   * c3.Obj.make({
   *   "type": 'User',
   *   "email": 'joe@smith.com',
   *   "realName": 'Joe Smith'
   * })
   *
   * c3.User(email='joe@smith.com', realName='Joe Smith')
   *
   * c3.Obj(type='User', email='joe@smith.com', realName='Joe Smith')
   * ```
   *
   * Note that this is **not** the same as the [serialization format](serdeser.c3doc). This is a convenient way to
   * specify fields and values in the "JSON like" form supported by each language, but the usual serialization rules,
   * such as {@link Ann.Ser} do not apply.
   * @param fields
   *            Fields to construct the instance of the obj with
   * @param withDefaults
   *            If set, then the Obj is made with default & initial values (required primitive fields e.g. !int32 -> 0) populated
   *
   * @see fromFields
   * @see beforeMake
   * @see afterMake
   * @see withDefaults
   */
  static make(fields: any, withDefaults?: boolean): EvalMetricsResult;

  /**
   * Construct an instance of this type from provided instance of a subtype or a "duck type".
   */
  static remake(other: Obj | null, failIfExtraOrInvalidFields?: boolean): EvalMetricsResult;

  /**
   * Optional override that will be called every time instance of this type is created.
   *
   *
   * Note that it introduces additional overhead so should only be implemented for low volume data.
   */
  static beforeMake(fields: C3.Map<FieldType | null, any> | null): C3.Map<FieldType | null, any>;

  /**
   * Optional override that will be called after every instance creation.
   *
   * Note that it introduces additional overhead so should only be implemented for low volume data.
   */
  afterMake(): EvalMetricsResult;

  /**
   * Creates an empty inst using `MyType.make()` and caches it. Avoid recreating multiple copies of the spec for
   * every action dispatch. The cached inst can also be used for comparing whether the object is an empty or not
   * Will only create empty instance for immutable Obj e.g. if an Obj is Mutable, this method will throw an error
   *
   * @see ValueType#defaultEmptyValue
   */
  static cachedEmptyInst(): EvalMetricsResult;

  /**
   * Converts metric evaluation results into a flattened {@link Data} table format.
   *
   * Transforms the nested result structure into a tabular format where each row
   * represents a single time point for a source-metric combination. The resulting
   * table includes columns for sourceId, timestamp, and each metric value.
   *
   * Example output structure:
   * ```
   * | sourceId  | start      | TotalConsumption | AvgConsumption |
   * |-----------|------------|------------------|----------------|
   * | building1 | 2010-01-01 | 3                | 0.9            |
   * | building1 | 2010-01-02 | 4                | 1.1            |
   * | building1 | 2010-01-03 | 5                | 2.1            |
   * | building2 | 2010-01-01 | 13               | 10.9           |
   * | building2 | 2010-01-02 | 14               | 10.1           |
   * | building2 | 2010-01-03 | 15               | 13.1           |
   * ```
   *
   * @return
   *    Flattened {@link Data} representation with time-series data in tabular format.
   *
   * @see #flattenedType
   * @see #result
   */
  toData(): Data | null;

  /**
   * Generate a stream of instances of this type. The stream is endless and will call #generateObj each time a new
   * value is read.
   */
  static generateObjs(spec?: Obj.GenerateSpec | null): Stream<EvalMetricsResult>;

  /**
   * Generate a single instance of this type. The base implementation uses {@link DataGenObj} to generate uniform
   * random (gibberish) values for all fields, but it may be overridden by specific types with custom logic that
   * populates fields in a more realistic way.
   */
  static generateObj(spec?: Obj.GenerateSpec | null): EvalMetricsResult;

  /**
   * Converts existing eval metrics result to a new one with the given interval and aggregation
   * @param interval
   *            The interval to convert to. If not provided, will choose the #mostFrequentInterval
   * @param agg
   *            Aggregation function per metric if we are merging features in this set by aggregating or disaggregating on time
   *            interval. If not provided, will choose default agg function of SUM
   * @return The new EvalMetricsResultBase with the given interval and aggregation
   */
  toInterval(interval?: string | null, agg?: C3.Map<string | null, string | null> | string | null | null): EvalMetricsResult | null;

  /**
   *  Converts existing eval metrics result to a new one with the given period and aggregation
   *
   *  For Example:
   *  ```
   *  emr.toPeriod("2W@5", "SUM") converts to biWeekly Period starting from Friday.
   *
   *  emr.toPeriod("Q@1", {"metric1": "SUM", "metric2": "AVG"}) converts to quarterly Period, with quarter starting from January
   *  metric1 is aggregated with SUM and metric2 is aggregated on AVG.
   *  ```
   *
   * For aggregation, all the data points in the period will be aggregated with the agg functions specified,
   * but for disaggregation, **the values are prorated depending on the data points in the interval** (in the below examples its the days in the interval)
   * ```js
   * ti = TimeInfo.from(DateTime.fromString("2023-04-01"), DateTime.fromString("2023-07-01"), Interval.DAY, TimeZone.NONE)
   * let arr = new Array(91);
   * arr.fill(1, 0, 30);
   * arr.fill(2, 30, 60);
   * arr.fill(3, 60, 91);
   * ts = Timeseries.fromValues(ti, arr).toDoubleTimeseries();
   * emr = C3.EvalMetricsResult.make({'result': {"source1": {"metric1": ts}}})
   * emr.toInterval('MONTH', 'SUM').toPeriod('W@2','SUM'))
   * ```
   *
   *  ```
   *  ----------------------------
   *     timestamp   |    value
   *     2023-03-28  |    3
   *     2023-04-04  |    7
   *         *       |    *
   *         *       |    *
   *     2023-04-25  |    8.03226
   *         *       |    *
   *         *       |    *
   *     2023-06-27  |    12
   *  ----------------------------
   *  ```
   * In the above example, the first data point will be for 2023-03-28, with value 3 as we only have 3 days of this week in the first month (30/30 * 3) = 3.
   * Week starting from 2023-04-25 has 6 days in the first month and 1 day in the next month, where the values are prorated depending on the days in the interval. (30/30 * 6) + (63/31 * 1) = 8.03226.
   * Week starting from 2023-06-27 only has 4 days in the interval with 4*(90/30) = 12
   *
   *  @param period
   *             The period to convert to.
   *  @param agg
   *             Aggregation function per metric if we are merging features in this set by aggregating or disaggregating on given
   *             period. If not provided, will choose default agg function of SUM
   *  @return The new EvalMetricsResultBase with the given period and aggregation
   */
  toPeriod(period: string, agg?: C3.Map<string | null, string | null> | string | null | null): EvalMetricsResult | null;

  /**
   * Filter the results by the given as of or masked metric in the Feature.Set and return only the filtered rows
   * as a stream of Pair<subject, [Timestamp, FeatureName]>. Result is keyed by subject
   */
  toDataColumns(set: Feature.Set): Stream<Pair<string | null, C3.Array<Data.Column | null>> | null>;

  /**
   * Filter the results by the given asOf or masked metric in the array of features and return only the filtered rows
   * as a stream of Pair<subject, [Timestamp, FeatureName]>
   * @param keep
   *            If keep is set to true, then mask Timeseries will be inverse of the dates / time range passed in the filter
   * @param filterMetric
   *            the metric to be used for masking other eval metric results.
   * @see Timeseries#convertToMask
   */
  toDataColumns(features: C3.Array<Feature | null>, keep?: boolean, filterMetric?: string | null): Stream<Pair<string | null, C3.Array<Data.Column | null>> | null>;

  /**
   * Find all the legacy features in the Feature.Set and include the Feature.Set's maskFeature or asOfFeature if it is not
   * declared in {@link Feature.Set#featureDecls}
   */
  static allAndFilterFeatures(set: Feature.Set): Pair<C3.Array<Feature | null>, string | null> | null;

  /**
   * Merge multiple EvalMetricsResultBase together.
   */
  merge(other?: EvalMetricsResult | null): EvalMetricsResult | null;

  /**
   * Provides an alternative view of evaluation results organized by result key rather than source.
   *
   * Transforms the default source-first organization from {@link #result}
   * (`sourceId -> resultKey -> Timeseries`) into a result-key-first organization
   * (`resultKey -> sourceId -> Timeseries`).
   *
   * Example transformation:
   * Original {@link #result} structure:
   * ```
   * {
   *   "srcId1": { "Metric1": Timeseries1, "Metric2": Timeseries2 },
   *   "srcId2": { "Metric1": Timeseries3, "Metric2": Timeseries4 }
   * }
   * ```
   *
   * Transformed byMetrics structure:
   * ```
   * {
   *   "Metric1": { "srcId1": Timeseries1, "srcId2": Timeseries3 },
   *   "Metric2": { "srcId1": Timeseries2, "srcId2": Timeseries4 }
   * }
   * ```
   *
   * This view is particularly useful for metric-centric analysis and visualization
   * where you want to compare the same metric across different sources.
   *
   * @return
   *    Results organized by result key, then by source ID.
   *
   * @see #result
   */
  byMetrics(): C3.Map<string | null, C3.Map<string | null, any>>;

  /**
   * Retrieves the {@link Timeseries} for a specific metric and source combination.
   *
   * @param metricName
   *    The result key for the desired {@link Timeseries}. This corresponds to the inner map key
   *    in {@link #result}. By default, this is the metric name from the original expression,
   *    but when {@link EvalMetricsSpec#resultKey} is specified in the evaluation specification,
   *    this should be the custom key generated by that lambda function.
   * @param srcId
   *    The source object ID for which the {@link Timeseries} is desired. Required.
   * @return
   *    The {@link Timeseries} for the specified metric and source combination.
   *
   * @see EvalMetricsSpec#resultKey
   * @see #result
   */
  timeseries(metricName: string, srcId: string): TimeseriesBase<any, TimeseriesBase.Info | null> | null;

  /**
   * Extracts all {@link Timeseries} objects from the evaluation results as a flat array.
   *
   * Iterates through all source-metric combinations in {@link #result} and collects
   * every {@link Timeseries} into a single array. Useful for bulk operations,
   * statistical analysis, or when you need to process all metric data regardless
   * of source or metric organization.
   *
   * @return
   *    Array containing all {@link Timeseries} objects from the evaluation results.
   *
   * @see #result
   * @see #timeseries
   */
  allTimeseries(): C3.Array<TimeseriesBase<any, TimeseriesBase.Info | null> | null>;

  /**
   * Generates a dynamic {@link Type} definition for flattened metric result data.
   *
   * Creates a {@link Type} schema that can be used to store the evaluation results
   * in a flattened, tabular format. The generated Type includes fields for source
   * identification, timestamps, and individual metric columns based on the specific
   * metrics present in the evaluation results.
   *
   * Generated Type structure:
   * ```
   * @db(partitionKeyField="sourceId")
   * type Obj_<DynamicTypeSuffix> mixes Partitionable<string> {
   *   sourceId: string
   *   start: datetime
   *   HeartRate: Timeseries<double>
   *   WbcCount: Timeseries<double>
   *   // ... additional fields for each metric in results
   * }
   * ```
   *
   * This Type can be used for persistent storage, data processing, or integration
   * with systems that require strongly-typed schemas for metric data.
   *
   * @return
   *    Dynamically generated {@link Type} definition suitable for storing flattened results.
   *
   * @see #toData
   * @see Partitionable
   */
  flattenedType(): Type;

  /**
   * Determines the most commonly used interval across all {@link Timeseries} in the results.
   *
   * When evaluation results contain {@link Timeseries} with different temporal intervals
   * (e.g., some hourly, some daily), this method analyzes all series and returns the
   * interval that appears most frequently. Useful for determining a common temporal
   * resolution for data processing or visualization operations.
   *
   * @return
   *    The most frequently occurring {@link Interval} among all {@link Timeseries} in the results.
   *
   * @see #allTimeseries
   * @see Interval
   */
  mostFrequentInterval(): string;
}


interface λFunction<T, R> {
  (t: T): R
}

interface λBiFunction<T, U, R> {
  (t: T, u: U): R
}

interface λQuadFunction<T, U, V, W, R> {
  (t: T, u: U, v: V, w: W): R
}

interface λPredicate<T> {
  (t: T): boolean
}
