// TypeScript definitions for the C3 type PartitionBucketStrategy

/**
 * Strategy to adopt while bucketing data within a partition for {@link Partitionable} types.
 * Uses {@link Ann.Db#persistenceOrder} to determine what field to bucket by. If not specified then buckets by id
 *
 * Spec defines ability to partition data in different underlying storage, viz:
 *
 * *Hot* - Low volume / Frequently accessed data. Hot storage is typically more expensive than cold storage, so the amount
 * of data should be capped in this datastore. Latencies to obtain data from hot store is less (faster) than if obtaining
 * from the cold data store
 *
 * *Cold* - Heavy volume / less frequently accessed data (slow moving - hardly changing). This is typically to park data
 * that is historical and is needed for some analysis, but is not frequently accessed and is heavy volume. Cold storage
 * is typically cheaper from a compute/storage costs but comes at higher latencies to access the data.
 *
 * By default, if no spec is provided, all objects will be stored in the hot storage & a default hot storage strategy
 * will be chosen. In order to share objects in between hot and cold storage, following techniques could be adopted:
 *
 * 1. Bucket in hot & cold store by size:
 * e.g. Let's say there is 10MB of data to be partitioned in a bucket. This example keeps only 1MB worth of data in
 * hot storage and will store all other data in cold storage (in 9 buckets) where each bucket size in cold storage will be ~1MB
 * (say default is 10x of size in hot bucket, this can be overridden by {@link coldBucketMultiple}
 * ```js
 *      {
 *          "sizeMbPerHotBucket" : 0.100 // ~ 100 kb per hot bucket
 *          "numHotBuckets" : 10 // will only keep at the max 10 hot buckets i.e. 1MB worth of data in hot storage
 *      }
 * ```
 *
 * 2. Bucket in hot & cold store by number of objects:
 * e.g. Let's say there is 10K objects to be partitioned in a bucket. This example keeps only 1K objects in
 * hot storage and will store all other data in cold storage (in 9 buckets) where each bucket contains 1K objects
 * (say default is 10x of size in hot bucket, this can be overridden by {@link coldBucketMultiple}
 * ```js
 *      {
 *          "numObjsPerHotBucket" : 100 // ~ 100 objects per hot bucket
 *          "numHotBuckets" : 10 // will only keep at the max 10 hot buckets i.e. 1K objects in hot storage
 *      }
 * ```
 *
 * 3. Bucket in hot & cold store by max hot sort key:
 * e.g. Let's say there is 1M timeseries objects of type {@link IntervalDataPoint} to be partitioned in a bucket. The
 * data points range from the year 2010 -> 2020 where each year contains 100k points (hypothetical scenario)
 * Let's say we want to keep only the last years data in the hot storage  store all other data in cold storage.
 * We can create a configuration as follows:
 * ```js
 *      {
 *          "numObjsPerHotBucket" : 10000 // ~ 10k objects per hot bucket
 *          "maxHotSortKey" : "start < (now() - period(1,'YEAR'))" // will only keep last 1 year worth of data in hot storage
 *      }
 * ```
 *
 * Note: The period when data gets compacted in the cold storage may be delayed
 *
 * @remarks this represents a value passed to a method that expects an instance of PartitionBucketStrategy
 */
declare interface IPartitionBucketStrategy {

  /**
   * number of new cols to read during fillBuckets operation - this limits the memory footprint in memory and limits
   * size of records written to cassandra during fillBuckets. Use -1 to read everything (with caution)
   */
  newColReadCount?: number | null;

  /**
   * Limit on the number of objects in the new col c__nc__ col to not blow up memory when #newColReadCount columns are
   * read in memory
   */
  newColMaxObjCount?: number | null;

  /**
   * If set, then retries {@link Partitionable#fillBuckets} or {@link Partitionable#compact} actions repeatedly until
   * it is fully compacted, up to {@link PartitionBucketStrategy#maxRetries}.
   * This will be done incrementally, so it will not use significant memory.
   * If #force or #fullCompact is set, then this is ignored
   */
  rewriteAllIncremental?: boolean;

  /**
   * The priority for invalidation queue entries generated by the job.  When null, the priority will be that specified
   * in @invalidationQueueOptions(priority).  This field will be modified by the setPriority function to override the
   * default.  Avoid setting manually as that will not update the priority of already submitted invalidations.
   */
  priority?: number | null;

  /**
   * Indicates that the job is in the disabled state meaning that no queue entries will be processed for the job.
   * This should only be set manually when initially creating/starting the job.  To disable/enable a running job call
   * {@link disable}/{@link enable}.
   */
  disabled?: boolean;

  /**
   * The maximum number of invalidation queue entries that will be processed simultaneously for a job.  If -1, no limit
   * is applied. This is enforced independently of {@link maxConcurrencyPerNode}.
   */
  maxConcurrency?: number | null;

  /**
   * The maximum number of invalidation queue entries that will be processed simultaneously for a job per node.  If -1,
   * no limit is applied.  This is enforced independently of {@link maxConcurrency}.
   */
  maxConcurrencyPerNode?: number | null;

  /**
   * If true, the appropriate functions (e.g. map/reduce in MapReduce and processBatch in BatchJob) will be run as the
   * user who started the job.  If false, they will be run as root.
   */
  runAsUser?: boolean | null;

  /**
   * When non-null, invalidation queue entries for the job will only be processed by servers with a matching
   * nodePool.  When null, invalidation queue entries for the job will only be processed by servers from default
   * nodePool.
   */
  nodePool?: string | null;

  /**
   * If true, attempts to start the job will be ignored if it is already running and no error will be thrown.  This is useful,
   * for instance, if a cron job starts a job and the job from a previous cron invocation may not have completed before the next
   * scheduled run.
   *
   * If false, an error will be thrown if an attempt is made to start the job if it is already running.
   */
  skipIfRunning?: boolean | null;

  /**
   * Set to between 1 and 5 to enable auto recovery. See {@link invalidation-queues.c3doc Invalidation Queues} for detailed documentation.
   */
  numAutoRecoveryAttempts?: number | null;

  /**
   * If true, the job will propagate {@link Action.Context}.
   * This should only be set manually when starting the job.
   */
  propagateActionContext?: boolean;

  /**
   * Target type ref - has to be persistable and has to mixin TargetType - if not provided then TargetType will
   * be used instead
   */
  targetType?: IType | null;

  /**
   * If true, the file system will be used for intermediate value storage.  Otherwise, the default KV store will
   * be used.
   */
  useFileSystemForInterVals?: boolean;

  /**
   * If true, the `batchIds` function will be called on the target type during `readInput` to allow for delegating the
   * i/o associated with reading the target objs to the map processing phase.  The `numBatches` or `batchSize` and
   * `filter` for the job run will be supplied as parameters to the `batchIds` call.
   *
   * IMPORTANT - See {@link Fetchable#batchIds} for information on how the `numBatches` and `batchSize` parameters are
   *             interpreted.
   *
   * Not valid if order is specified
   */
  useBatchIdsForReadInput?: boolean;

  /**
   * include spec for c3 Objs passed to map function as inValue array
   *
   * default is TargetType @db(partitionKeyField) or "id" if partitionKeyField is not specified
   *
   * if include is a single field path then only unique values will be present in c3 Objs in inValue array
   */
  include?: string | null;

  /**
   * optional filter spec
   */
  filter?: string | null;

  /**
   * optional order
   *
   * if not specified, order will be by the id of the target type.
   */
  order?: string | null;

  /**
   * optional number of TargetType objs to process; actual number processed will be exactly this number (or
   * less if there is less data)
   *
   * only valid when order is specified
   */
  limit?: number | null;

  /**
   * optional percentage of TargetType objs to process; actual number will be approximate
   *
   * only valid when order and useBatchIdsForReadInput not specified
   */
  samplePct?: number | null;

  /**
   * number of TargetType instances to pass to map function as inValue array; inKey is going to be batch index
   * starting with 1;
   *
   * if numBatches is not specified, batch size received by map function will be approximate;
   *
   * default is 1000
   *
   * not valid when numBatches is specified
   */
  batchSize?: number | null;

  /**
   * optional subBatchSize
   *
   * if specified then the map function for a single batch will be be called multiple times with the subBatchSize
   * number of entries; each call will have the same inKey corresponding to the batch
   */
  subBatchSize?: number | null;

  /**
   * optional total number of batches to break the target data into (e.g. the number of dispatchMap entries that
   * will be put in the MapReduceQueue)
   *
   * if specified, batchSize will be ignored
   */
  numBatches?: number | null;

  /**
   * optional flag to abort on first error (by default on first error job will continue processing all maps or
   *
   * reduces and will simply mark state as "failing" - it can be recovered from this state using "recover" action)
   */
  abortOnError?: boolean;

  /**
   * If set, only places the specified number of objects per hot bucket
   */
  numObjsPerHotBucket?: number | null;

  /**
   * If set, attempts to hold as many objects as can fit in the allowed size in Mbs. The size in every bucket will be close
   * to the one specified but is not guaranteed to be exactly the same
   */
  sizeMbPerHotBucket?: number | null;

  /**
   * If set, buckets objects in the specified number of buckets. Can be used in conjunction with {@link numObjsPerHotBucket}
   * & {@link numHotBuckets}, and this will limit the max number of hot buckets. If not specified, a default number of
   * hot buckets will be created
   */
  numHotBuckets?: number | null;

  /**
   * Max value across all the buckets in hot storage. E.g. max date until which data should be in hot storage (e.g. "start < (now() - period(6, 'MONTH'))"
   * Only supports filter on {@link Ann.Db#persisenceOrder} for the given type
   */
  maxHotSortKey?: string | null;

  /**
   * Multiplier from each hot bucket size. If hot buckets are partitioned by {@link numObjsPerHotBucket} then this multiplier
   * will result in max object count in each cold bucket. If it is partitioned by {@link sizeMbPerHotBucket} then
   * this multiplier will result in max size in mb in each cold bucket. Of course, the number of objects and size could
   * be less if the total number of objects in cold bucket is fewer than the max capacity.
   * If set, determines the size of each cold bucket & the total number of cold buckets depending on remaining data from
   * hot + size of each cold bucket
   */
  coldBucketMultiplier?: number | null;

  /**
   * Schedule at which compaction needs to run. {@see DataPartition#compact}. Defaults to 1st every month at 00:00
   */
  compactionSchedule?: string | null;

  /**
   * If set, no error will be thrown if packing fails and will log an error instead.
   */
  doNotThrowOnFailure?: boolean;

  /**
   * Determines how long to continuously pack if {@link PartitionBucketNewColSpec#rewriteAllIncremental} is true.
   * Set to -1 to pack as long as nc columns exist, but use with caution!
   */
  maxRetries?: number | null;

  /**
   * **CAUTION** For Internal Use only
   * If set, performs full compaction {@see DataPartition#fullCompact}. This operation is costly and reads all the data
   * (not incremental) and tries to stick to the spec as close as possible. Regular compaction is incremental & tries
   * to touch as little data as needed but may end up with not perfectly sized buckets
   */
  fullCompact?: boolean;

  /**
   * **CAUTION** For Internal Use only
   * If set, the state column of the partition will be re-written based on the source of truth currently in the row.
   * This will reconcile orphaned hot buckets and remove references to buckets that no longer exist.
   */
  repair?: boolean;
}

/**
 * Strategy to adopt while bucketing data within a partition for {@link Partitionable} types.
 * Uses {@link Ann.Db#persistenceOrder} to determine what field to bucket by. If not specified then buckets by id
 *
 * Spec defines ability to partition data in different underlying storage, viz:
 *
 * *Hot* - Low volume / Frequently accessed data. Hot storage is typically more expensive than cold storage, so the amount
 * of data should be capped in this datastore. Latencies to obtain data from hot store is less (faster) than if obtaining
 * from the cold data store
 *
 * *Cold* - Heavy volume / less frequently accessed data (slow moving - hardly changing). This is typically to park data
 * that is historical and is needed for some analysis, but is not frequently accessed and is heavy volume. Cold storage
 * is typically cheaper from a compute/storage costs but comes at higher latencies to access the data.
 *
 * By default, if no spec is provided, all objects will be stored in the hot storage & a default hot storage strategy
 * will be chosen. In order to share objects in between hot and cold storage, following techniques could be adopted:
 *
 * 1. Bucket in hot & cold store by size:
 * e.g. Let's say there is 10MB of data to be partitioned in a bucket. This example keeps only 1MB worth of data in
 * hot storage and will store all other data in cold storage (in 9 buckets) where each bucket size in cold storage will be ~1MB
 * (say default is 10x of size in hot bucket, this can be overridden by {@link coldBucketMultiple}
 * ```js
 *      {
 *          "sizeMbPerHotBucket" : 0.100 // ~ 100 kb per hot bucket
 *          "numHotBuckets" : 10 // will only keep at the max 10 hot buckets i.e. 1MB worth of data in hot storage
 *      }
 * ```
 *
 * 2. Bucket in hot & cold store by number of objects:
 * e.g. Let's say there is 10K objects to be partitioned in a bucket. This example keeps only 1K objects in
 * hot storage and will store all other data in cold storage (in 9 buckets) where each bucket contains 1K objects
 * (say default is 10x of size in hot bucket, this can be overridden by {@link coldBucketMultiple}
 * ```js
 *      {
 *          "numObjsPerHotBucket" : 100 // ~ 100 objects per hot bucket
 *          "numHotBuckets" : 10 // will only keep at the max 10 hot buckets i.e. 1K objects in hot storage
 *      }
 * ```
 *
 * 3. Bucket in hot & cold store by max hot sort key:
 * e.g. Let's say there is 1M timeseries objects of type {@link IntervalDataPoint} to be partitioned in a bucket. The
 * data points range from the year 2010 -> 2020 where each year contains 100k points (hypothetical scenario)
 * Let's say we want to keep only the last years data in the hot storage  store all other data in cold storage.
 * We can create a configuration as follows:
 * ```js
 *      {
 *          "numObjsPerHotBucket" : 10000 // ~ 10k objects per hot bucket
 *          "maxHotSortKey" : "start < (now() - period(1,'YEAR'))" // will only keep last 1 year worth of data in hot storage
 *      }
 * ```
 *
 * Note: The period when data gets compacted in the cold storage may be delayed
 *
 * @remarks this represents a made instance of PartitionBucketStrategy
 */
declare class PartitionBucketStrategy extends Obj {

  /**
   * number of new cols to read during fillBuckets operation - this limits the memory footprint in memory and limits
   * size of records written to cassandra during fillBuckets. Use -1 to read everything (with caution)
   */
  readonly newColReadCount?: number | null;
  withNewColReadCount(newColReadCount: number | null): PartitionBucketStrategy;

  /**
   * Limit on the number of objects in the new col c__nc__ col to not blow up memory when #newColReadCount columns are
   * read in memory
   */
  readonly newColMaxObjCount?: number | null;
  withNewColMaxObjCount(newColMaxObjCount: number | null): PartitionBucketStrategy;

  /**
   * If set, then retries {@link Partitionable#fillBuckets} or {@link Partitionable#compact} actions repeatedly until
   * it is fully compacted, up to {@link PartitionBucketStrategy#maxRetries}.
   * This will be done incrementally, so it will not use significant memory.
   * If #force or #fullCompact is set, then this is ignored
   */
  readonly rewriteAllIncremental?: boolean;
  withRewriteAllIncremental(rewriteAllIncremental: boolean): PartitionBucketStrategy;

  /**
   * The priority for invalidation queue entries generated by the job.  When null, the priority will be that specified
   * in @invalidationQueueOptions(priority).  This field will be modified by the setPriority function to override the
   * default.  Avoid setting manually as that will not update the priority of already submitted invalidations.
   */
  readonly priority?: number | null;
  withPriority(priority: number | null): PartitionBucketStrategy;

  /**
   * Indicates that the job is in the disabled state meaning that no queue entries will be processed for the job.
   * This should only be set manually when initially creating/starting the job.  To disable/enable a running job call
   * {@link disable}/{@link enable}.
   */
  readonly disabled?: boolean;
  withDisabled(disabled: boolean): PartitionBucketStrategy;

  /**
   * The maximum number of invalidation queue entries that will be processed simultaneously for a job.  If -1, no limit
   * is applied. This is enforced independently of {@link maxConcurrencyPerNode}.
   */
  readonly maxConcurrency?: number | null;
  withMaxConcurrency(maxConcurrency: number | null): PartitionBucketStrategy;

  /**
   * The maximum number of invalidation queue entries that will be processed simultaneously for a job per node.  If -1,
   * no limit is applied.  This is enforced independently of {@link maxConcurrency}.
   */
  readonly maxConcurrencyPerNode?: number | null;
  withMaxConcurrencyPerNode(maxConcurrencyPerNode: number | null): PartitionBucketStrategy;

  /**
   * If true, the appropriate functions (e.g. map/reduce in MapReduce and processBatch in BatchJob) will be run as the
   * user who started the job.  If false, they will be run as root.
   */
  readonly runAsUser?: boolean | null;
  withRunAsUser(runAsUser: boolean | null): PartitionBucketStrategy;

  /**
   * When non-null, invalidation queue entries for the job will only be processed by servers with a matching
   * nodePool.  When null, invalidation queue entries for the job will only be processed by servers from default
   * nodePool.
   */
  readonly nodePool?: string | null;
  withNodePool(nodePool: string | null): PartitionBucketStrategy;

  /**
   * If true, attempts to start the job will be ignored if it is already running and no error will be thrown.  This is useful,
   * for instance, if a cron job starts a job and the job from a previous cron invocation may not have completed before the next
   * scheduled run.
   *
   * If false, an error will be thrown if an attempt is made to start the job if it is already running.
   */
  readonly skipIfRunning?: boolean | null;
  withSkipIfRunning(skipIfRunning: boolean | null): PartitionBucketStrategy;

  /**
   * Set to between 1 and 5 to enable auto recovery. See {@link invalidation-queues.c3doc Invalidation Queues} for detailed documentation.
   */
  readonly numAutoRecoveryAttempts?: number | null;
  withNumAutoRecoveryAttempts(numAutoRecoveryAttempts: number | null): PartitionBucketStrategy;

  /**
   * If true, the job will propagate {@link Action.Context}.
   * This should only be set manually when starting the job.
   */
  readonly propagateActionContext?: boolean;
  withPropagateActionContext(propagateActionContext: boolean): PartitionBucketStrategy;

  /**
   * Target type ref - has to be persistable and has to mixin TargetType - if not provided then TargetType will
   * be used instead
   */
  readonly targetType?: Type | null;
  withTargetType(targetType: IType | null): PartitionBucketStrategy;

  /**
   * If true, the file system will be used for intermediate value storage.  Otherwise, the default KV store will
   * be used.
   */
  readonly useFileSystemForInterVals?: boolean;
  withUseFileSystemForInterVals(useFileSystemForInterVals: boolean): PartitionBucketStrategy;

  /**
   * If true, the `batchIds` function will be called on the target type during `readInput` to allow for delegating the
   * i/o associated with reading the target objs to the map processing phase.  The `numBatches` or `batchSize` and
   * `filter` for the job run will be supplied as parameters to the `batchIds` call.
   *
   * IMPORTANT - See {@link Fetchable#batchIds} for information on how the `numBatches` and `batchSize` parameters are
   *             interpreted.
   *
   * Not valid if order is specified
   */
  readonly useBatchIdsForReadInput?: boolean;
  withUseBatchIdsForReadInput(useBatchIdsForReadInput: boolean): PartitionBucketStrategy;

  /**
   * include spec for c3 Objs passed to map function as inValue array
   *
   * default is TargetType @db(partitionKeyField) or "id" if partitionKeyField is not specified
   *
   * if include is a single field path then only unique values will be present in c3 Objs in inValue array
   */
  readonly include?: string | null;
  withInclude(include: string | null): PartitionBucketStrategy;

  /**
   * optional filter spec
   */
  readonly filter?: string | null;
  withFilter(filter: string | null): PartitionBucketStrategy;

  /**
   * optional order
   *
   * if not specified, order will be by the id of the target type.
   */
  readonly order?: string | null;
  withOrder(order: string | null): PartitionBucketStrategy;

  /**
   * optional number of TargetType objs to process; actual number processed will be exactly this number (or
   * less if there is less data)
   *
   * only valid when order is specified
   */
  readonly limit?: number | null;
  withLimit(limit: number | null): PartitionBucketStrategy;

  /**
   * optional percentage of TargetType objs to process; actual number will be approximate
   *
   * only valid when order and useBatchIdsForReadInput not specified
   */
  readonly samplePct?: number | null;
  withSamplePct(samplePct: number | null): PartitionBucketStrategy;

  /**
   * number of TargetType instances to pass to map function as inValue array; inKey is going to be batch index
   * starting with 1;
   *
   * if numBatches is not specified, batch size received by map function will be approximate;
   *
   * default is 1000
   *
   * not valid when numBatches is specified
   */
  readonly batchSize?: number | null;
  withBatchSize(batchSize: number | null): PartitionBucketStrategy;

  /**
   * optional subBatchSize
   *
   * if specified then the map function for a single batch will be be called multiple times with the subBatchSize
   * number of entries; each call will have the same inKey corresponding to the batch
   */
  readonly subBatchSize?: number | null;
  withSubBatchSize(subBatchSize: number | null): PartitionBucketStrategy;

  /**
   * optional total number of batches to break the target data into (e.g. the number of dispatchMap entries that
   * will be put in the MapReduceQueue)
   *
   * if specified, batchSize will be ignored
   */
  readonly numBatches?: number | null;
  withNumBatches(numBatches: number | null): PartitionBucketStrategy;

  /**
   * optional flag to abort on first error (by default on first error job will continue processing all maps or
   *
   * reduces and will simply mark state as "failing" - it can be recovered from this state using "recover" action)
   */
  readonly abortOnError?: boolean;
  withAbortOnError(abortOnError: boolean): PartitionBucketStrategy;

  /**
   * If set, only places the specified number of objects per hot bucket
   */
  readonly numObjsPerHotBucket?: number | null;
  withNumObjsPerHotBucket(numObjsPerHotBucket: number | null): PartitionBucketStrategy;

  /**
   * If set, attempts to hold as many objects as can fit in the allowed size in Mbs. The size in every bucket will be close
   * to the one specified but is not guaranteed to be exactly the same
   */
  readonly sizeMbPerHotBucket?: number | null;
  withSizeMbPerHotBucket(sizeMbPerHotBucket: number | null): PartitionBucketStrategy;

  /**
   * If set, buckets objects in the specified number of buckets. Can be used in conjunction with {@link numObjsPerHotBucket}
   * & {@link numHotBuckets}, and this will limit the max number of hot buckets. If not specified, a default number of
   * hot buckets will be created
   */
  readonly numHotBuckets?: number | null;
  withNumHotBuckets(numHotBuckets: number | null): PartitionBucketStrategy;

  /**
   * Max value across all the buckets in hot storage. E.g. max date until which data should be in hot storage (e.g. "start < (now() - period(6, 'MONTH'))"
   * Only supports filter on {@link Ann.Db#persisenceOrder} for the given type
   */
  readonly maxHotSortKey?: string | null;
  withMaxHotSortKey(maxHotSortKey: string | null): PartitionBucketStrategy;

  /**
   * Multiplier from each hot bucket size. If hot buckets are partitioned by {@link numObjsPerHotBucket} then this multiplier
   * will result in max object count in each cold bucket. If it is partitioned by {@link sizeMbPerHotBucket} then
   * this multiplier will result in max size in mb in each cold bucket. Of course, the number of objects and size could
   * be less if the total number of objects in cold bucket is fewer than the max capacity.
   * If set, determines the size of each cold bucket & the total number of cold buckets depending on remaining data from
   * hot + size of each cold bucket
   */
  readonly coldBucketMultiplier?: number | null;
  withColdBucketMultiplier(coldBucketMultiplier: number | null): PartitionBucketStrategy;

  /**
   * Schedule at which compaction needs to run. {@see DataPartition#compact}. Defaults to 1st every month at 00:00
   */
  readonly compactionSchedule?: string | null;
  withCompactionSchedule(compactionSchedule: string | null): PartitionBucketStrategy;

  /**
   * If set, no error will be thrown if packing fails and will log an error instead.
   */
  readonly doNotThrowOnFailure?: boolean;
  withDoNotThrowOnFailure(doNotThrowOnFailure: boolean): PartitionBucketStrategy;

  /**
   * Determines how long to continuously pack if {@link PartitionBucketNewColSpec#rewriteAllIncremental} is true.
   * Set to -1 to pack as long as nc columns exist, but use with caution!
   */
  readonly maxRetries?: number | null;
  withMaxRetries(maxRetries: number | null): PartitionBucketStrategy;

  /**
   * **CAUTION** For Internal Use only
   * If set, performs full compaction {@see DataPartition#fullCompact}. This operation is costly and reads all the data
   * (not incremental) and tries to stick to the spec as close as possible. Regular compaction is incremental & tries
   * to touch as little data as needed but may end up with not perfectly sized buckets
   */
  readonly fullCompact?: boolean;
  withFullCompact(fullCompact: boolean): PartitionBucketStrategy;

  /**
   * **CAUTION** For Internal Use only
   * If set, the state column of the partition will be re-written based on the source of truth currently in the row.
   * This will reconcile orphaned hot buckets and remove references to buckets that no longer exist.
   */
  readonly repair?: boolean;
  withRepair(repair: boolean): PartitionBucketStrategy;

  /**
   * Load the JSON-based representation and reconstruct the corresponding object.
   *
   * fromJson is be called on the type be deserialized and must reconstruct an Obj of the appropriate type (which may be
   * a type that mixes in the type on which it is called). This means that the resulting object's type will be isA the
   * called-on type, but perhaps not identical. In particular, `Obj.fromJson` works for any actual type and will return
   * an instance of the correct type.
   *
   * @see #toJson
   */
  static fromJson(json: any | null): PartitionBucketStrategy | null;

  /**
   * Load the JSON-based representation and reconstruct the corresponding object.
   *
   * fromJsonString is be called on the type be deserialized and must reconstruct an Obj of the appropriate type (which may be
   * a type that mixes in the type on which it is called). This means that the resulting object's type will be isA the
   * called-on type, but perhaps not identical. In particular, `Obj.fromJsonString` works for any actual type and will return
   * an instance of the correct type.
   *
   * @see #toJsonString
   */
  static fromJsonString(json: string | null): PartitionBucketStrategy | null;

  /**
   * Load the XML-based representation and reconstruct the corresponding object.
   *
   * fromXmlString is be called on the type be deserialized and must reconstruct an Obj of the appropriate type (which
   * may be a type that mixes in the type on which it is called). This means that the resulting object's type will be
   * isA the called type, but perhaps not identical. In particular, `Obj.fromXmlString` works for any actual type and
   * will return an instance of the correct type.
   *
   * @see #toXmlString
   */
  static fromXmlString(xml: string | null): PartitionBucketStrategy | null;

  /**
   * Load from contentType representation and reconstruct the corresponding object.
   *
   * fromString is be called on the type be deserialized and must reconstruct an object of the appropriate type
   * (which may be a type that mixes in the type on which it is called). This means that the resulting object's type
   * will be isA the called-on type, but perhaps not identical. In particular, `fromString` works for any actual
   * type and will return an instance of the correct type.
   */
  static deserialize(contentStr: string | null, contentType: string): PartitionBucketStrategy | null;

  /**
   * Returns new instance with all references to old type, including result of #type, replaced with new type. If new
   * type does not contain fields from old or field value types are not convertable then drops the field.
   *
   * This method is used during live metadata update
   */
  replaceType(old: Type, new_: Type): PartitionBucketStrategy;

  /**
   * Result of this function call is a copy of current instance with all non empty fields replaced based on results of
   * the `mapper` invocation.
   *
   * @param action
   *           lambda to apply for every field value to produce a new value for that field
   * @param convertValue
   *           if true, attempt to convert the value to match the field's type
   */
  mapFieldValues(mapper: λBiFunction<FieldType, any, any>, convertValue?: boolean): PartitionBucketStrategy;

  /**
   * Result of this function call is a copy of current instance with all fields replaced based on results of the
   * `mapper` invocation.
   *
   * @param spec
   *           which fields to include
   * @param mapper
   *           lambda to apply for every field value to produce a new value for that field
   * @param convertValue
   *           if true, attempt to convert the value to match the field's type
   */
  mapFieldValues(spec: ValueSpec, mapper: λBiFunction<FieldType, any, any>, convertValue?: boolean): PartitionBucketStrategy;

  /**
   * Result of this function call is a copy of current instance with all non empty fields replaced based on results of
   * the asynchronous `mapper` invocation.
   *
   * @param action
   *           lambda to apply for every field value to produce a new value for that fields
   * @param convertValue
   *           if true, attempt to convert the value to match the field's type
   */
  mapFieldValuesAsync(mapper: λBiFunction<FieldType, any, C3.Promise<any> | null>, convertValue?: boolean): C3.Promise<PartitionBucketStrategy | null>;

  /**
   * Result of this function call is a copy of current instance with all fields replaced based on results of the
   * asynchronous `mapper` invocation.
   *
   * @param spec
   *           which fields to include
   * @param mapper
   *           lambda to apply for every field value to produce a new value for that field
   * @param convertValue
   *           if true, attempt to convert the value to match the field's type
   */
  mapFieldValuesAsync(spec: ValueSpec, mapper: λBiFunction<FieldType | null, any, C3.Promise<any> | null>, convertValue?: boolean): C3.Promise<PartitionBucketStrategy | null>;

  /**
   * Result of this function call is a copy of current instance with specified field value replaced based on result of
   * the `mapper` invocation.
   * @param field
   *           field being mapped
   * @param includeEmpty
   *           if set, invokes mapper for fields with empty value
   * @param mapper
   *           lambda to apply for every field value to produce a new value for that field
   * @param convertValue
   *           if true, attempt to convert the value to match the field's type
   */
  mapFieldValue(field?: FieldType | null, includeEmpty?: boolean, mapper: λFunction<any, any>, convertValue?: boolean): PartitionBucketStrategy;

  /**
   * Executes the specified lambda against each referenced Obj instance and replaces it's value with result of this
   * lambda application.
   *
   * Result of this function call is a copy of current instance with all references replaced based on results of the
   * `mapper` invocation.
   *
   * @param action
   *           function to be executed for each pair of field type and Obj instance
   * @param convertValue
   *           if true, attempt to convert the value to match the field's type
   */
  mapRefs(mapper: λBiFunction<FieldType, Obj, Obj | null>, convertValue?: boolean): PartitionBucketStrategy;

  /**
   * Executes the specified lambda against each referenced Obj instance and replaces it's value with result of this
   * lambda application.
   *
   * Result of this function call is a copy of current instance with all references replaced based on results of the
   * `mapper` invocation.
   *
   * @param includeEmpty
   *           if `true` will also process references with `null` / "Empty" references
   * @param mapper
   *           function to be executed for each pair of field type and Obj instance for producing new reference value
   * @param convertValue
   *           if true, attempt to convert the value to match the field's type
   */
  mapRefs(includeEmpty: boolean, mapper: λBiFunction<FieldType, Obj, Obj | null>, convertValue?: boolean): PartitionBucketStrategy;

  /**
   * Populates all missing default values and throws error if any constraint is violated.
   */
  validateObj(): PartitionBucketStrategy;

  /**
   * Builds a new Obj instance by adding the provided field in it. The name must correspond to an existing field
   * defined on this type or its mixins. The value must be of the correct type if doNotConvert flag is true.
   *
   * @param field
   *           name of the field
   * @param value
   *           of the field
   * @param doNotConvert
   *           if true, do not attempt to convert the value to match the field's type
   * @return new Obj
   *
   * @see #withoutField
   * @see #defaultField
   */
  withField(field: string, value: any, doNotConvert?: boolean): PartitionBucketStrategy;

  /**
   * Builds a new Obj instance by adding the provided field in it. The name must correspond to an existing field
   * defined on this type or its mixins. The value must be of the correct type if doNotConvert flag is true.
   *
   * @param field
   *           the field
   * @param value
   *           of the field
   * @param doNotConvert
   *           if true, do not attempt to convert the value to match the field's type
   * @return new Obj
   *
   * @see #withoutField
   * @see #defaultField
   */
  withField(field: FieldType, value: any, doNotConvert?: boolean): PartitionBucketStrategy;

  /**
   * Builds a new Obj instance by adding the provided fields in it. The name must correspond to an existing fields
   * defined on this type or its mixins. The values must be of the correct type if doNotConvert flag is true.
   *
   * @param fields
   *           map of field names/values
   * @param doNotConvert
   *           if true, attempt to convert the values to match the fields' type
   * @return new Obj
   */
  withFields(fields: C3.Map<string | null, any> | null, doNotConvert?: boolean): PartitionBucketStrategy;

  /**
   * Builds a new Obj with the value at the specified path field. If the field is null, the field #isFieldSet to null.
   * If you would like to #unsetField, you should call #withoutFieldAtPath instead.
   *
   * Immutable objects may return the same instance if the field being set does not actually represent a
   * change to the existing object.
   *
   * @param path
   *           path to set value at
   * @param value
   *           value to set
   * @param doNotConvert
   *           if true, attempt to convert the values to match the fields' type
   * @param doNotCreateIfMissing
   *           true indicates that any empty reference along the path will not set the value
   * @return new Obj
   */
  withFieldAtPath(path: string, value: any, doNotConvert?: boolean, doNotCreateIfMissing?: boolean): PartitionBucketStrategy;

  /**
   * Builds a new Obj without the specified path field.
   *
   * Immutable objects may return the same instance if the field being removed does not actually represent a
   * change to the existing object.
   *
   * @param path
   *           path for field to remove
   * @return new Obj
   *
   * @see #withFieldAtPath
   * @see #withoutField
   */
  withoutFieldAtPath(path: string): PartitionBucketStrategy;

  /**
   * Builds a new Obj, removing the field with the provided name.
   *
   * Immutable objects may return the same instance if the field being removed is not present in the existing object.
   *
   * @param field
   *           name of the field to remove
   * @return new Obj with removed field
   *
   * @see #unsetField
   * @see #removeField
   */
  withoutField(field: string | null): PartitionBucketStrategy;

  /**
   * Builds a new Obj, removing the field with the provided field type.
   *
   * Immutable objects may return the same instance if the field being removed is not present in the existing object.
   *
   * @param field
   *           name of the field to remove
   * @return new Obj with removed field
   *
   * @see #unsetField
   * @see #removeField
   */
  withoutField(field: FieldType | null): PartitionBucketStrategy;

  /**
   * Builds a new Obj, removing the fields with the provided names.
   *
   * Immutable objects may return the same instance if the fields being removed are not present in the existing object.
   *
   * @param fields
   *           names of the fields to remove
   * @return new Obj with removed fields
   */
  withoutFields(fields: C3.Array<string | null> | null): PartitionBucketStrategy;

  /**
   * Builds a new Obj, removing the fields with the provided field types. Be sure to use the FieldType instance for the
   * exact same type as the type of the obj to respect the "ordinal" of the field type
   *
   * Immutable objects may return the same instance if the fields being removed are not present in the existing object.
   *
   * @param fields
   *           field types to remove
   * @return new Obj with removed fields
   */
  withoutFieldsByType(fields: C3.Array<FieldType | null> | null): PartitionBucketStrategy;

  /**
   * @return a new Obj, removing the field types marked with annotation @config(secret=true) recursively
   */
  withoutSecretFields(): PartitionBucketStrategy;

  /**
   * Builds a new Obj instance by adding the default values (if defined) for all unset fields. This is implemented by
   * calling {@link FieldType#defaultValue defaultValue} for a field if it is not already set and
   * {@link FieldType#hasDefault has a default}. It will also set {@see ValueType#initialValue initial values} for
   * fields with required primitive ValueTypes (E.g. x: `!int32` -> will be set to 0). Note that this will not overwrite
   * fields that have already been set.
   *
   * {@link FunctionParam#validateArg} will call {@link #withDefaults} for {@link Spec}s passed as arguments to methods.
   * As a result, methods should be implemented assuming all default values are set on `Spec` arguments.
   *
   * @param includeEmptyRefsWithDefaults
   *           it `true` then missing / empty child references that have fields with defaults will also be instantiated
   * @param defaultFields
   *           If not empty, a list of default field paths to populate.  Any default fields not specified in the
   *           array will be ignored.
   * @return new Obj
   *
   * @see #defaultField
   * @see FieldType#defaultValueConst
   * @see FieldType#defaultValue
   */
  withDefaults(includeEmptyRefsWithDefaults?: boolean, defaultFields?: C3.Array<string | null>): PartitionBucketStrategy;

  /**
   * Builds a new Obj, by setting a field on this `Obj` to the field's default value. If the field has no default, this
   * method will behave the same as {@link #unsetField}.
   *
   * @param field
   *         name of the field to default
   * @return new `Obj` with the specified field set to its default value
   *
   * @see #withField
   * @see #unsetField
   */
  defaultField(field: string): PartitionBucketStrategy;

  /**
   * Builds a new Obj, by setting a field on this `Obj` to the fields default value. If the field has no default, this
   * method will behave the same as {@link #unsetField}.
   *
   * @param field
   *         field type to default
   * @return new `Obj` with the specified field set to its default value
   *
   * @see #withField
   * @see #unsetField
   */
  defaultField(field: FieldType): PartitionBucketStrategy;

  /**
   * Unsets a field from this `Obj`, meaning that the field will become not {@link #isFieldSet set}. Note that this
   * is different from {@link removeField}
   *
   * @param field
   *         name of the field to unset
   * @return new `Obj` with the specified field unset
   *
   * @see #withoutField
   * @see #removeField
   */
  unsetField(field: string): PartitionBucketStrategy;

  /**
   * Unsets a field from this `Obj`, meaning that the field will become not {@link #isFieldSet set}. Note that this
   * is different from {@link removeField}
   *
   * @param field
   *         field type to unset
   * @return new `Obj` with the specified field unset
   *
   * @see #withoutField
   * @see #removeField
   */
  unsetField(field: FieldType): PartitionBucketStrategy;

  /**
   * Removes a field from this `Obj`, meaning that the field will become {@link isFieldMissing missing}. Note that this
   * is different from {@link #unsetField}
   *
   * @param field
   *         name of the field to remove
   * @return new `Obj` with the specified field removed
   *
   * @see #withoutField
   * @see #unsetField
   */
  removeField(field: string): PartitionBucketStrategy;

  /**
   * Removes a field from this `Obj`, meaning that the field will become {@link isFieldMissing missing}. Note that this
   * is different from {@link #unsetField}
   *
   * @param field
   *         field type to remove
   * @return new `Obj` with the specified field removed
   *
   * @see #withoutField
   * @see #unsetField
   */
  removeField(field: FieldType): PartitionBucketStrategy;

  /**
   * Merges all the fields of the provided Obj into this instance, producing a new Obj of the same type as this one.
   * In case of conflicts, fields of other instance take precedence unless otherwise specified by the fieldPathMergeSpec
   *
   * @param other
   *           object
   * @param fieldPathMergeSpec
   *           mapping of fields of the object to the respective merge annotations
   * @return the new merged Obj
   */
  mergeObj(other: Obj | null, fieldPathMergeSpec?: C3.Map<string | null, string | null>): PartitionBucketStrategy;

  /**
   * Merges all the fields of the provided Obj into this instance, producing a new Obj of the same type as this one.
   * In case of conflicts, fields of other instance take precedence.
   *
   * @param other
   *           object
   * @param otherFieldsFilter
   *           only fields of otherFieldsFilter type from other are merged into this obj.
   * @return the new merged Obj
   */
  mergeObj(other: Obj | null, otherFieldsFilter: Type): PartitionBucketStrategy;

  /**
   * Merge the fields of this Obj with corresponding fields on other Obj using the provided lambda. This means that
   * fields that exist on other Obj and do not exist on this Obj will not be added to final Obj.
   * @param deep
   *        if set to true then traverse reference and collection fields and merge corresponding fields or elements with
   *        the same key or index.
   */
  mergeObj(other: Obj | null, deep?: boolean, merger: λQuadFunction<FieldPath | null, any, FieldPath | null, any, any>): PartitionBucketStrategy;

  /**
   * Merge the fields of this Obj with corresponding fields on other Obj using the provided lambda. This means that
   * fields that exist other Obj and do not exist on this Obj will not be added to final Obj or evaluated. Does not
   * traverse child reference and collection fields.
   */
  mergeObj(other: Obj | null, merger: λQuadFunction<FieldType | null, any, FieldType | null, any, any>): PartitionBucketStrategy;

  mergeJson(json: any | null): PartitionBucketStrategy;

  /**
   * Merge the obj references within the current obj
   * @param deep
   *        If set, traverses the reference fields within the obj as well for a deep merge
   * @param objKey
   *        lambda specifying how to obtain the key for the Obj while determining which Objs to merge
   * @param filter
   *        Field paths that need to be filtered from this merge
   * @return Obj with child references merged
   */
  mergeChildren(deep?: boolean, objKey?: λFunction<Obj | null, any> | null, filter?: λPredicate<string> | null): PartitionBucketStrategy;

  /**
   * Adds the numeric Obj fields with the other Objs respective fields.
   * If deep is set it will traverse reference and collection fields and sum corresponding numeric fields in
   * references with same name and collection elements at same index or key.
   */
  sumObj(other: Obj | null, deep?: boolean): PartitionBucketStrategy;

  /**
   * Build an array of the correct type with a single element which is this instance.
   *
   * @return new array instance with this as only element.
   */
  singletonArray(): C3.Array<PartitionBucketStrategy | null>;

  /**
   * Creates an array of instances of this type.
   */
  static array(...elements: any[]): C3.Array<PartitionBucketStrategy | null>;

  /**
   * Creates an array of instances of this type.
   */
  static arrayBuilder(): ArrayBuilder<PartitionBucketStrategy | null> | null;

  /**
   * Build an set of the correct type with a single element which is this instance.
   *
   * @return new array instance with this as only element.
   */
  singletonSet(): C3.Set<PartitionBucketStrategy | null>;

  /**
   * Creates a set of instances of this type.
   */
  static setBuilder(): SetBuilder<PartitionBucketStrategy | null> | null;

  /**
   * Create a map of string to elements of this type.
   */
  static mapBuilder(): MapBuilder<string | null, PartitionBucketStrategy | null> | null;

  /**
   * Create a map with the given key type and elements of this type.
   */
  static mapBuilderOf(keyType: ValueType): MapBuilder<any, PartitionBucketStrategy | null> | null;

  /**
   * @return new ObjBuilder with initial state set to fields of this instance.
   */
  toBuilder(): ObjBuilder<PartitionBucketStrategy | null>;

  /**
   * @return new ObjBuilder of this instance.
   */
  static builder(): ObjBuilder<PartitionBucketStrategy | null>;

  /**
   * Construct instance of this type from provided field values and options
   */
  static fromFields(fields: C3.Map<FieldType | null, any> | null, spec: Obj.MakeSpec | null): PartitionBucketStrategy;

  /**
   * Construct an instance of this type from provided fields
   * @param fields
   *            Fields to construct the instance of the obj with
   * @param withDefaults
   *            If set, then the Obj is made with default & initial values (required primitive fields e.g. !int32 -> 0) populated
   *
   * @see withDefaults
   */
  static fromFields(fields: C3.Map<FieldType | null, any | any | null> | null, withDefaults?: boolean): PartitionBucketStrategy;

  /**
   * Construct instance of this type from provided field values and options
   */
  static make(fields: C3.Map<string | null, any> | null, spec: Obj.MakeSpec | null): PartitionBucketStrategy;

  /**
   * Construct an instance of this type with no non-default field values unless explicitly specified by passing param true
   * @param withDefaults
   *            If set, then the Obj is made with default & initial values (required primitive fields e.g. !int32 -> 0)
   *            populated
   *
   * @see withDefaults
   */
  static make(withDefaults?: boolean): PartitionBucketStrategy;

  /**
   * Construct an instance from provided fields
   * @param fields
   *            Fields (in the format <field_name, value>) to construct an instance of the obj. Note that "type" as a
   *            field_name will be considered as the actual Obj's type, e.g. Obj.make({"type": "Panda"}) is equivalent
   *            to Panda.make()
   * @param withDefaults
   *            If set, then the Obj is made with default & initial values (required primitive fields e.g. !int32
   *            -> 0) populated. Passing an empty value for a field will result in the initial value being set if
   *            the field does not {@link ValueModifier#PRESERVES_EMPTY preserve empty}
   *
   *
   * @see fromFields
   * @see beforeMake
   * @see afterMake
   * @see withDefaults
   */
  static make(fields: C3.Map<string | null, any | any | null> | null, withDefaults?: boolean): PartitionBucketStrategy;

  /**
   * Construct an instance of this type from provided fields. Note it is more efficient to use #fromFields and other overloads
   *
   * ```js
   * User.make({
   *   email: 'joe@smith.com',
   *   realName: 'Joe Smith'
   * })
   *
   * Obj.make({
   *   type: 'User',
   *   email: 'joe@smith.com',
   *   realName: 'Joe Smith'
   * })
   * ```
   *
   * ```py
   * c3.User.make({
   *   "email": 'joe@smith.com',
   *   "realName": 'Joe Smith'
   * })
   *
   * c3.Obj.make({
   *   "type": 'User',
   *   "email": 'joe@smith.com',
   *   "realName": 'Joe Smith'
   * })
   *
   * c3.User(email='joe@smith.com', realName='Joe Smith')
   *
   * c3.Obj(type='User', email='joe@smith.com', realName='Joe Smith')
   * ```
   *
   * Note that this is **not** the same as the [serialization format](serdeser.c3doc). This is a convenient way to
   * specify fields and values in the "JSON like" form supported by each language, but the usual serialization rules,
   * such as {@link Ann.Ser} do not apply.
   * @param fields
   *            Fields to construct the instance of the obj with
   * @param withDefaults
   *            If set, then the Obj is made with default & initial values (required primitive fields e.g. !int32 -> 0) populated
   *
   * @see fromFields
   * @see beforeMake
   * @see afterMake
   * @see withDefaults
   */
  static make(fields: any, withDefaults?: boolean): PartitionBucketStrategy;

  /**
   * Construct an instance of this type from provided instance of a subtype or a "duck type".
   */
  static remake(other: Obj | null, failIfExtraOrInvalidFields?: boolean): PartitionBucketStrategy;

  /**
   * Optional override that will be called every time instance of this type is created.
   *
   *
   * Note that it introduces additional overhead so should only be implemented for low volume data.
   */
  static beforeMake(fields: C3.Map<FieldType | null, any> | null): C3.Map<FieldType | null, any>;

  /**
   * Optional override that will be called after every instance creation.
   *
   * Note that it introduces additional overhead so should only be implemented for low volume data.
   */
  afterMake(): PartitionBucketStrategy;

  /**
   * Creates an empty inst using `MyType.make()` and caches it. Avoid recreating multiple copies of the spec for
   * every action dispatch. The cached inst can also be used for comparing whether the object is an empty or not
   * Will only create empty instance for immutable Obj e.g. if an Obj is Mutable, this method will throw an error
   *
   * @see ValueType#defaultEmptyValue
   */
  static cachedEmptyInst(): PartitionBucketStrategy;

  /**
   * Generate a stream of instances of this type. The stream is endless and will call #generateObj each time a new
   * value is read.
   */
  static generateObjs(spec?: Obj.GenerateSpec | null): Stream<PartitionBucketStrategy>;

  /**
   * Generate a single instance of this type. The base implementation uses {@link DataGenObj} to generate uniform
   * random (gibberish) values for all fields, but it may be overridden by specific types with custom logic that
   * populates fields in a more realistic way.
   */
  static generateObj(spec?: Obj.GenerateSpec | null): PartitionBucketStrategy;
}


interface λFunction<T, R> {
  (t: T): R
}

interface λBiFunction<T, U, R> {
  (t: T, u: U): R
}

interface λQuadFunction<T, U, V, W, R> {
  (t: T, u: U, v: V, w: W): R
}

interface λPredicate<T> {
  (t: T): boolean
}
