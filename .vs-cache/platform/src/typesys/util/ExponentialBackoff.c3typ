/*
 * Copyright 2009-2025 C3 AI (www.c3.ai). All Rights Reserved.
 * This material, including without limitation any software, is the confidential trade secret and proprietary
 * information of C3 and its licensors. Reproduction, use and/or distribution of this material in any form is
 * strictly prohibited except as set forth in a written license agreement with C3 and/or its authorized distributors.
 * This material may be covered by one or more patents or pending patent applications.
 */

/**
 * An exponential back-off sequence generator. This has the property of a predictable exponential
 * growth curve defined by initialMillis * (exponent ^ attempt), but with actual
 * wait times randomly distributed between 0 and this curve.
 */
type ExponentialBackoff mixes Spec {

  /**
   * The base delay in milliseconds.
   * The nth sleep time is approximately calculated as [baseMillis * exponent ^ n] with jitter.
   * If both baseMillis and initialMillis are provided, baseMillis takes priority.
   */
  baseMillis: int

  /**
   * The exponent to multiply the prior wait time in generating the next.
   * Note that 1.0 will generate a linear wait time.
   * Note that 2.0 will double the maximum possible wait each time.
   */
  exponent: !double = 2.0

  /**
   * The base delay in milliseconds, used to set the base of the series. It does not send an 'initial' wait time.
   */
  @deprecated(details='Use baseMillis instead.')
  initialMillis: int = 10

  /**
   * Determines the algorithm used to calculate jitter. If noJitter is set to true, jitter will always be set to NONE.
   */
  jitter: ExponentialBackoff.Jitter = FULL

  /**
   * The maximum number of values to generate, representing the maximum number of retries in the sequence.
   * If unset, an infinite number of values will be generated.
   */
  maxRetry: int

  /**
   * The maximum total time of all retries to generate. If unset, an infinite total time of values will be generated.
   * Note that regardless of the total time, at least one value will be generated.
   */
  maxTotalMillis: int

  /**
   * The maximum number of milliseconds for each sleep iteration.  If not set, no limit will be imposed.
   */
  maxSleepMillis: int

  /**
   * The minimum time guaranteed each iteration.
   */
  minMillis: int

  /**
   * If for some reason you do not want jitter (not recommended) turn it off by setting this to true.
   * Then for each step (if no maximums are exceeded) you will wait exactly initialMillis * (exponent ^ attempt)
   * instead of the default which is randomly chosen on a uniform distribution between 0 and
   * initialMillis * (exponent ^ attempt). This default is the "Full Jitter" recommended strategy for making the
   * fewest number of overall calls described by the {@link
   * https://www.awsarchitectureblog.com/2015/03/backoff.html AWS Architecture Blog}.
   */
  @deprecated(details='Use jitter instead.')
  noJitter: boolean

  /**
   * Return true if maxRetry and/or maxTotalMillis is specified, providing an upper bound on the number of retries.
   */
  isBounded: member function(): boolean

  /**
   * Produce a stream of values. This stream may be infinite in length if both maxRetry and maxTotalMillis are unset.
   */
  toStream: member function(): stream<int>

  /**
   * Produce an array of values. At least one of maxRetry or maxTotalMillis must have been specified (otherwise the
   * array would be of infinite length).
   */
  toArray: member function(): [int]

  /**
   * Produce the nth value of the sequence. Note that if maxRetry or maxTotalMillis is specified, this method will
   * return null after those limits are reached.
   *
   * @param attempt 0-based index of next retry
   */
  nth: member function(attempt: !int): int

  iterator: private member function(): @java(class="c3.platform.typesys.value.Iterator<Long>") native
}
