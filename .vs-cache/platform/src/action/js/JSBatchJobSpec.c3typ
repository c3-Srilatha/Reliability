/*
 * Copyright 2009-2025 C3 AI (www.c3.ai). All Rights Reserved.
 * This material, including without limitation any software, is the confidential trade secret and proprietary
 * information of C3 and its licensors. Reproduction, use and/or distribution of this material in any form is
 * strictly prohibited except as set forth in a written license agreement with C3 and/or its authorized distributors.
 * This material may be covered by one or more patents or pending patent applications.
 */

type JSBatchJobSpec mixes BatchJobOptions, Spec {

  /**
   * JS function code that implements doStart logic. (required)
   */
  doStart: !string

  /**
   * JS function code that implements processBatch logic. (required)
   */
  processBatch: !string

  /**
   * JS function code that implements allComplete logic. (optional)
   */
  allComplete: string

  /**
   * Optional context object if ad-hoc job needs additional options.
   */
  context: Obj

  /**
   * By default job will abort on errors. If you set this flag then on failure job will move into failing
   * state and will stay in the queue in failed state. It can then be recovered at a later point.
   */
  doNotAbortOnError: boolean
}
