/*
 * Copyright 2009-2026 C3 AI (www.c3.ai). All Rights Reserved.
 * This material, including without limitation any software, is the confidential trade secret and proprietary
 * information of C3 and its licensors. Reproduction, use and/or distribution of this material in any form is
 * strictly prohibited except as set forth in a written license agreement with C3 and/or its authorized distributors.
 * This material may be covered by one or more patents or pending patent applications.
 */

/**
 * Define extended UI attributes on types, fields, and methods. Provides display hints for any UI context including
 * grids, forms, and other visualizations.
 */
@annotation(appliesTo=['default-ui', 'default-ui-field', 'field', 'function'])
type Ann.Ui mixes Annotation {

  /**
   * Path to image used by UI element.
   */
  image: string

  /**
   * Expose this property or function in the UI as though it were a field. This only works for methods which can be
   * called with no arguments.
   */
  show: boolean

  /**
   * Exclude this field, property, or method from UI display. Overrides {@link #show} if both are specified.
   */
  hide: boolean

  /**
   * Force the alignment of the element. If not specified, numeric values are typically right-aligned and others
   * are left-aligned.
   */
  align: string enum('left', 'right')

  /**
   * Custom display label. If not specified, the field or method name is used.
   */
  label: string
}
