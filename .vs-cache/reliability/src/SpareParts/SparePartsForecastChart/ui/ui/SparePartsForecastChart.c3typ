/*
 * Copyright 2009-2025 C3 AI (www.c3.ai). All Rights Reserved.
 * Confidential and Proprietary C3 Materials.
 * This material, including without limitation any software, is the confidential trade secret and proprietary
 * information of C3 and its licensors. Reproduction, use and/or distribution of this material in any form is
 * strictly prohibited except as set forth in a written license agreement with C3 and/or its authorized distributors.
 * This material may be covered by one or more patents or pending patent applications.
 */

/**
 * The UI component that handles showing Spare Parts Inventory and Forecast data in a chart.
 */
/*
 * c3-analysis-disable c3-excessive-method-params -- existing functions should not cause error
 */
type SparePartsForecastChart mixes UiSdlTimeseriesLineBarChart {

  /**
   * The id of the component to show when there is no data.
   */
  noDataComponentId: string

  /**
   * The {@link InventorySeries#partNumber} of the part currently plotted on the chart.
   */
  selectedPartNumber: string

  /**
   * The start of the zoom range.
   */
  zoomRangeStart: datetime

  /**
   * The end of the zoom range.
   */
  zoomRangeEnd: datetime

  /**
   * The range of data that the `data` field contains.
   * Has two keys, start and end.
   */
  dataRange: map<string, datetime>

  /**
   * The chart data to display.
   */
  chartData: private SparePartsForecastChartData

  /**
   * Fetches the data to display on the chart.
   *
   * @param componentId
   *        The {@link InventorySeries#id} of the component to affect.
   * @param partId
   *        The {@link InventorySeries#partNumber} to fetch data for.
   * @param partNumber
   *        The {@link InventorySeries#partNumber} to display in the subtitle
   * @param useDefaultTimeRange
   *        If true, use the default time range that is read from (@link ReliabilitySparePartsConfig#lookBackTimeWindow} and {@link ReliabilitySparePartsConfig#lookAheadTimeWindow}.
   *        If false, read the time range from `Reliability.SparePartsForecastChart.displayStartDate` and `Reliability.SparePartsForecastChart.displayEndDate`.
   * @return A Redux action to get data
   */
  @uiSdlActionCreator(actionType='GET_DATA')
  getDataAction: function(componentId: !string, partId: string, partNumber: string, useDefaultTimeRange: boolean): !UiSdlReduxAction ts-client

  /**
   * When the `GET_DATA` action is triggered, this epic will set the `data` field with the returned data from
   * {@link SparePartsForecastChartDataGetter#getData}.
   *
   * @param actionStream
   *           The observable stream of UI SDL actions
   * @param stateStream
   *           The observable stream of UI SDL states
   * @return An observable stream of UI SDL actions
   */
  @uiSdlEpic(actionType='GET_DATA')
  getDataEpic: private  function(
    actionStream: !UiSdlActionsObservable,
    stateStream: !UiSdlStatesObservable
  ): !UiSdlActionsObservable ts-client

  /**
   * Resets the chart to its state after `GET_DATA` action was called.
   * @param componentId
   *        The id of the component to affect.
   * @return A Redux action to reset the chart
   */
  @uiSdlActionCreator(actionType='RESET_CHART')
  resetChartAction: function(componentId: !string): !UiSdlReduxAction ts-client

  /**
   * When the `RESET_CHART` action is triggered, this reducer will reset the chart to its state after
   * `GET_DATA` action was called.
   *
   * @param state
   *           The current Redux state
   * @param action
   *           The Redux action to reset the chart
   * @return A new Redux state with reset chart data
   */
  @uiSdlReducer(actionType='RESET_CHART')
  resetChartReducer: private function(state: !UiSdlReduxState, action: !UiSdlReduxAction): !UiSdlReduxState ts-client

  /**
   * Clears the chart of all data.
   * @param componentId
   *        The id of the component to affect.
   * @return A Redux action to clear the chart
   */
  @uiSdlActionCreator(actionType='CLEAR_CHART')
  clearChartAction: function(componentId: !string): !UiSdlReduxAction ts-client

  /**
   * When the `CLEAR_CHART` action is triggered, this reducer will clear the chart of all data.
   *
   * @param state
   *           The current Redux state
   * @param action
   *           The Redux action to clear the chart
   * @return A new Redux state with cleared chart data
   */
  @uiSdlReducer(actionType='CLEAR_CHART')
  clearChartReducer: private function(state: !UiSdlReduxState, action: !UiSdlReduxAction): !UiSdlReduxState ts-client

  /**
   * @param componentId
   *        The id of the component to affect.
   * @param pathToValue
   *        Map of path in component that should be set, to value that should be set.
   * @return A Redux action to set fields
   */
  @uiSdlActionCreator(actionType='SET_FIELDS')
  setFieldsAction: function(componentId: !string, pathToValue: map<string ,any>): !UiSdlReduxAction ts-client

  /**
   * When the `SET_FIELDS` action is triggered, this reducer will update the state by setting the specified fields in the component.
   *
   * @param state
   *           The current Redux state
   * @param action
   *           The Redux action containing field updates
   * @return A new Redux state with updated fields
   */
  @uiSdlReducer(actionType='SET_FIELDS')
  setFieldsReducer: private function(state: !UiSdlReduxState, action: !UiSdlReduxAction): !UiSdlReduxState ts-client

  /**
   * Triggered to update the interval selector in the chart.
   *
   * @param id
   *           Id of the component.
   * @param start
   *           Start date for the filter spec {@link EvalMetricSpec}.
   * @param end
   *           End date for the filter spec.
   * @param interval
   *           interval for the filter spec.
   * @return A display date update action
   */
  @uiSdlActionCreator(actionType='DISPLAY_DATE_UPDATE')
  updateDisplayDateAction: function(id: string,
                                    start: string,
                                    end: string,
                                    interval: string): UiSdlDisplayDateUpdateAction ts-client

  /**
   * Triggered to update the filter for the time series and get the data.
   *
   * @param id
   *           Id of the component.
   * @param start
   *           Start date for the filter spec {@link EvalMetricSpec}.
   * @param end
   *           End date for the filter spec.
   * @param interval
   *           interval for the filter spec.
   * @return A Redux action to get data with updated interval
   */
  @uiSdlActionCreator(actionType='GET_DATA')
  getDataFromIntervalChangeAction: function(id: string,
                                   start: string,
                                   end: string,
                                   interval: string): UiSdlReduxAction ts-client
}

/*
 * c3-analysis-enable c3-excessive-method-params
 */
