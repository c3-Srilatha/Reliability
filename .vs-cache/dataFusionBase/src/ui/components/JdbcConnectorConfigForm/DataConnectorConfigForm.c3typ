/*
 * Copyright 2009-2025 C3 AI (www.c3.ai). All Rights Reserved.
 * This material, including without limitation any software, is the confidential trade secret and proprietary
 * information of C3 and its licensors. Reproduction, use and/or distribution of this material in any form is
 * strictly prohibited except as set forth in a written license agreement with C3 and/or its authorized distributors.
 * This material may be covered by one or more patents or pending patent applications.
 */

/**
 * This component renders a form for configuring JDBC connector properties.
 */
@typeScript
type DataConnectorConfigForm mixes UiSdlComponent<UiSdlNoData> {

  /**
   * A record of properties to be displayed in the form.
   * Each property contains metadata like description, required status, and possible choices.
   * The form will render appropriate input fields based on the property type and configuration.
   */
  properties: map<string, map<string, any>>

  /**
   * The prefix for the JDBC URL (e.g., "jdbc:mysql://").
   * This is used to construct and validate the complete JDBC URL.
   */
  jdbcUrlPrefix: string

  /**
   * Whether the form is in a loading state.
   * When true, displays a loading spinner instead of the form.
   */
  loading: boolean

  /**
   * Information card props of type InformationCard.propTypes
   */
  informationCardProps: json

  /**
   * Whether the form is in edit mode.
   * When true, indicates that the user is editing an existing data connector configuration.
   */
  editMode: boolean

  /**
   * Fields that have been edited by the user.
   * Used to track which fields are in an edited state.
   * Key is the field name, and value is the new value entered by the user.
   */
  editedFields: map<string, string>

  /**
   * Triggered when form status changes.
   *
   * @param componentId
   *            Id of the component.
   * @param properties
   *           The map of field names to field values
   *
   * @returns A `INPUT_CHANGE` action for this instance.
   */
  @uiSdlActionCreator(actionType='INPUT_CHANGE')
  inputChangeAction: function(componentId: !string,
                              properties: map<string, map<string, string>>): !UiSdlReduxAction ts-client
}
